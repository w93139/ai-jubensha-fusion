# M3 用户阅读与交互反馈（2026-09-08）

用户已开始验证，授权修复 11 项反馈。目标是在现有本机入口交付可阅读的排版、正文末推进、可随时打开的底部发言与提问、逐行显示以及私人笔记，并准确解释选角、原件阶段、回忆、答卷与 TTS 增量。

## 范围与完成条件

- 阅读层安全解析标题、强调、列表；修复 OCR 中文硬折行，保留原文查看。不更改候选、原件、模型上下文或任何评分事实。
- 隐藏内部版本号；故事时间与现实时间区分；阅读材料后推进。原件称两场（两个阶段），页面五步为已采用的流程拆分。
- 公开发言/追问底部可展开收起，页面仍可滚动；清楚区分保存发言、请求角色回应、终局封卷。
- 新接收且通过服务端检查的角色台词逐行显示，支持跳过；刷新旧记录直接阅读，不重发模型请求。回忆仍由服务端实际事件授予。
- 私人笔记按登录用户/局/角色隔离，本机保存、导出、材料收藏与已解锁回忆集合；未认证或身份失效不显示旧笔记。
- 下拉采用已有可访问组件，保留键盘与禁用状态。
- 当前验证固定唐小姐；五角色 M4、语音输入/TTS、模型语义改进和正式发布不在此轮。

## 验证

新增合成文字/播放/笔记测试；完整既有前端测试、tsc、修改文件 lint、webpack 构建；仓库 dev_preflight 及 16 自测、后端安全默认检查、diff --check。独立只读复核。Ego 浏览器验证桌面和 390px 阅读、下拉、笔记保存恢复和底部交互；既有服务与用户进度保留，仅更新前端构建指纹。记录真实模型调用前后差值，历史整局不重跑。

## 风险与边界

笔记仅本机，不能宣称跨设备同步；音频按当局可听台词字符计费，不按整本输入 tokens，不播放其他人私聊。OCR 只修显示，不自动纠正文字事实。AI 回复与评分必须沿用既有权限、回执与确定性规则。最终交付保留既有模型语义未通过项。

验证结果与用户操作方法在完成时追加。

## 实际交付

已完成安全阅读格式、明确 OCR 标题层级、中文硬折行修复和原始排版入口；版本号隐藏、故事时间说明、阅读末推进和终局跳转；底部展开式发言/追问、统一 Radix 下拉；只消费当前玩家可听台词的逐行显示；按账号/局/角色隔离的随身手记。原件阶段与当前结构化答卷的差异已核对，MVP 按用户本轮要求暂不接入 TTS。

最终前端 280 项、后端 2503 项、类型检查、8 个修改源码文件 lint 无警告、预检及 16 自测、webpack 构建和 diff --check 通过。独立只读复核 Blocker 0 / Important 0；修复了收藏多余字段、英文/Unicode 数字的断行边界及下拉保留值碰撞。

浏览器实际使用 1440×1000 和 390×844：标题 20px/700、原断词连读、内部版本隐藏、正文末推进、无横向溢出、底部面板可收起且背景可滚动、手记入口不被挡住、角色列表键盘选择、笔记刷新恢复与 Escape 焦点返回通过。专用开发局新增 2 条实际模型回复；第二条记录到逐行、暂停、全部显示及显示完前不可收藏全文。新回忆显示时序由合成测试覆盖，这两条实测没有新回忆授予；未冒充全关键词验收。手记导出使用真实 Blob 的自动测试覆盖。

本轮新增真实调用 2，保守估算 ¥0.02780208；本机验证服务累计 5 次、¥0.06566376。旧完整套件及模型版本、后端服务与用户旧局保持；只切换了已验证的前端构建，旧构建留作备份。最终构建 EdlEi9fBfldGDNBcXW-9L，与启动器及 HTTP 清单摘要一致。

入口 http://127.0.0.1:18032/ 。仓库外 `m3-user-validation/验证流程.md` 已更新为本轮 11 项问题与验证流程，`feedback-validation-report.json` 和 `feedback-browser-*.json` 留存实际证据。浏览器采用 DOM、计算样式和交互证据，不冒充截图逐像素视觉验收。

M3 继续等待用户体验验证；先修用户反馈，再做 M4 五角色自由选角和各自整局试玩。旧模型内容遗漏、确定性升级和电话重复等问题仍保留，未宣称全部语义通过；runtime_ready=false、无正式发布。建议开发思考强度极高：角色可见性、回忆、答卷和费用边界需联合核对。
