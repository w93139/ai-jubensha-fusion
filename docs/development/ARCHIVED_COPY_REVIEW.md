# 待合并开发副本审查

审查日期：2026-09-05。主仓库 HEAD 为 `343689a`，现有未提交安全修复保留；没有执行合并、覆盖、提交或迁移。

审查对象是父目录 `存档/待合并开发副本/ai-jubensha-fusion` 中相对其 HEAD 的独有 schema、migration、tests 和接线差异，而非重新比较所有参考项目。

| 独有部分 | 实际能力与冲突 | 本次决定 |
| --- | --- | --- |
| `backend/src/schemas/juben_design.py` | `juben-design/1.0` 创作蓝图；检查部分列表非空、类型题材和知识→真相事件引用。没有严格嵌套类型、重复 ID、完整外键、来源页码、阶段权限、不可变版本；部分错误类型会在 `.get()` 时异常 | 复用“知识有来源、结构检查先于发布”的思路；不把此校验当作原剧本导入门禁 |
| `d2e3f4a5b6c7_add_juben_design_document.py` | 在可变 `scripts` 上加 JSON 列，上游是 `h8b9c0d1e2f3`；主线现已有 `i9c0d1e2f3a4` | 不直接合并，避免产生第二迁移头和把创作草稿混入运行时。新候选表迁移接在当前主线后 |
| `ScriptDBModel` / `ScriptInfo` / `ScriptRepository` | 将自由字典 `design_document` 接入旧创作存储；没有版本冻结或源文件清单 | 不加入旧全本 API；候选包与旧剧本表分开 |
| `FusionGameService.validate_script` | 在现有发布检查中增加蓝图检查与 mystery 限制；仍没有 Audit、人审、哈希批准 | 不覆盖当前服务安全改动；新导入服务没有发布能力，旧发布入口的限制继续单独标明 |
| `script_generation_agent.py` | 增加生成蓝图工具、欢乐/情感创作分支和角色生成规则 | 不接线。当前目标是按原商业剧本运行，不扩大到生成/改写业务 |
| `test_juben_design.py` 与改动过的生成/游戏测试 | 有 3 个蓝图用例及生成工具顺序、基础发布回归；不能证明原剧本来源/权限/版本边界 | 逐项阅读，未运行其旧全量测试或复制 fixtures；本轮在主仓库新增独立虚构候选包测试 |

结果：本工作包没有需要直接迁入的兼容实现。历史副本完整保留，未来若恢复创作功能，应独立审查；不能把本记录理解为其已合并或可删除。

后续实现见[候选包与导入记录](PHASE_2_PACKAGE_INTAKE.md)。
