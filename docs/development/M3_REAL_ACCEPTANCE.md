# M3 一次真实整局验收

本批已获得用户“同意，继续开发”授权并执行，因第7次非OK结果按约停止，状态FAILED。上一批M2的20次请求已经结束，本批独立冻结，不能恢复旧请求或沿用旧费用收据。完整工程进展见[M3记录](M3_FULL_TEXT_PLAY.md)。

## 固定执行范围

- 当前《孽岛疑云》采用版，一局，一名脚本控制的唐小姐和四名真实AI角色；不把脚本控制真人席称作M4用户试玩。
- 火山方舟北京渠道，固定`doubao-seed-character-260628`。只发送当次角色获准的文本与实际听到的发言，原始图片、其他角色私本和最终答案键不整体发送。
- 四AI独立调查投票，平票按冻结规则处理；每轮调查安排公共交流和至多8次电话单步判断；最后四AI独立提交个人答卷、指认和信任。真人席只提出普通问题、按可见选项操作，答卷统一明确不知道，不给AI灌入标准答案。
- 一次执行凭据；最多200次请求、3,000,000 tokens，估算费用预算5元。每次调用前预占，达到额度停止；无自动重试、提示迭代或跨渠道回退。
- 第一条非OK回执、超时、异常或预算不足即停止。合法“不确定”是已保存回答，仍须随后复核；不能把格式成功当作推理正确。
- 独立SQLite与测试发布器，不修改.env，不迁移主数据库，不作商业发布或追加后台模型审核。

## 费用依据

采用2026-09-05已保存的[火山方舟官方价格表](https://docs.volcengine.com/docs/82379/1544106?lang=zh)快照，所有请求统一按较高的32k–128k输入档估算：输入1.20元、缓存命中0.16元、输出6.00元/百万tokens。输入受65536字节约束；正式输出最多4096 tokens，普通对白1000 tokens。不创建付费持久缓存。

5元是本批程序控制的保守估算预算，不是模型效果承诺；实际消耗按原始用量收据保存，未收到结果按预占处理，不为未知请求再次扣费重发。价格证据时间明确保留，不冒称当天重新核价。

## 验收与完成判断

完整记录应包括两轮调查、实际角色交流、五席封卷、个人得分与结局、保存重进、每次用量与失败状态。逐条对照当时输入，检查角色知识、引用归属、推断与事实区分、私聊隔离和关键内容遗漏；低分本身不判失败，无依据捏造、泄露或无法完成则记为问题。未实际发生的回忆/电话分支保留未覆盖。

正式执行前固定候选、来源包、控制器、相关代码、依赖锁及安装版本的摘要；普通和Python优化模式均须通过授权/额度拒绝反例，最终摘要须跑完零网络的模拟演练。仓库外批次目录为`private-data/import-jobs/m3-full-play/20260907-adopted-working/real-acceptance-v1`，执行凭据已经占用，批次不可恢复或重跑。

只在实际模型走完全局、费用恢复检查和独立语义复核都有证据后，才能关闭M3。下一步M4再验证五种真人选角及用户体验；开发建议极高，复杂权限/结算终审可用Ultra。

## 执行前准备结果（历史）

最终批次摘要`8b828e904ef6647460ca602db2e3e855b679d4c8175a146c67f4050738717454`已核对候选、来源、代码与依赖身份。零网络完整演练完成29调查、228步骤、384保存事件、156次模拟SDK；五席封卷、计分/结局与新数据库会话恢复一致。普通和Python优化模式的5项保护反例各自通过；两项独立复核问题已修复。上述为执行前的模拟结果，当时真实请求0；之后已创建实际批准及单次执行凭据，见下文。

## 实际执行与结论

实际7次请求、55430 tokens，保守账本¥0.0706632，无剩余预占；0次调查、9控制步骤、16保存事件，未到结算。第7次电话提问basis为空，INVALID拒收；模型服务正常stop，不是渠道不可用。独立内容评估001/005/006通过、002有不确定性措辞风险、003经历冲突、004错误目击归属及保密要求遗漏、007问题预设缺依据，本批失败。

原始输入/输出、approval、execution-claimed、失败收据均留在原目录。真实存档隔离副本的重放、同键检查和免费释放通过：不调用SDK、不改变费用，原16事件不改。新代码可恢复旧事件，但不能用旧授权追加新请求。

离线修复与新候选校正见[M3记录](M3_FULL_TEXT_PLAY.md)。后续独立的[4例真实语义复测](M3_SPEECH_REGRESSION.md)已获授权，在第2例错误引用后停止，1通过/1失败/2未测；离线目录修复已完成，真实整局仍未通过。
