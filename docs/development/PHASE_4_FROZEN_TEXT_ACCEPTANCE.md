# 阶段 4G：固定正文与真实模型验收

2026-09-06。1.4 的真实 Compiler 确认、服务端原样装配、来源核验及原固定小样本质量检查通过；后续 Audit 因第二条发现的 sources 字段不符合契约被拒绝，整条任务仍 BLOCKED。没有人工批准或发布，runtime_ready=false。

## 本轮修复

首次用 4F 的 1.3 流程真实请求 Qwen 时，规则字段没有改变，但 knowledge/evidence/truth 的玩家文字混入了原资料中的管理说明。这些说明确实来自其引用段落，所以通用原文校验通过；原固定小样本的精确质量门槛报 SMOKE_PRIVATE_FACT_MISMATCH，Audit 未发送。通用校验不能自行理解任意“正文/管理说明”边界。

新增显式 `compiler_mode=CONFIRM_FROZEN_TEXT`，请求/模型契约 1.4，输出为 `compiler-text-confirmation/1.0`。准备稿本来已包含完整且带来源的正文，因此新模式只让模型检查准备稿并返回槽位确认或阻断问题，不再让它重抄文字。模型返回槽位时不能带 text、sources 或规则字段；服务器使用整个准备稿原样生成候选。正文、规则或元数据不符的候选在复核时拒绝。

这不是自动生成或自动审核通过：准备稿可能存在错误，模型检查也可能遗漏，仍须完整 Audit 与人审发布门禁。旧 1.3 行为、失败候选和收据均保留，不使用新提示恢复旧任务。新模式的工作台、专用排队接口和显式 worker 参数均已接线，详见[契约补充](../contracts/frozen-rule-plan.md)。

## 真实结果（虚构材料，独立 SQLite）

资料内容沿用原 4D 固定合成文本，hash 为 `00ee9d21453f4e2221901f70f77808969309a3cdfa8e02bb7bd6954570c4c303`。规则准备稿是新任务的明确输入，hash 为 `a50470e1cde5d60ebfe646cd167a285417f4a382e1d4d06cdfd5e6eeff11daa3`，不是生成失败后的补写结果。没有商业素材外发。

| 流程 | 真实请求 | 结果 | 已知用量估算费用 |
| --- | --- | --- | --- |
| 1.3 规则冻结、模型摘录 | Compiler 1 次 | 夹带管理说明，精确质量门槛阻断；Audit 0 次 | ¥0.0008172 |
| 1.4 规则与正文固定、模型检查 | Compiler 1 次 | 候选与准备稿逐字段相同，原质量标准通过 | ¥0.0005954 |
| 1.4 后续 Audit | Audit 1 次 | AUDIT_OUTPUT_INVALID；`AUDIT_SCHEMA_INVALID /findings/1/sources` | ¥0.0028208 |

合计 **3 次真实请求，已知费用估算 ¥0.0042334，累计预占 ¥0.0235712**。无思考或缓存用量，无未知用量，无自动重发。单步骤预算仍 ¥0.05、单任务 ¥0.10；本轮两次零调用预览与两次独立真实任务都保留。费率按[阿里官方 Qwen3.7 Flash 页面](https://help.aliyun.com/zh/model-studio/qwen3-7-flash)的北京输入 ≤32K 档核验：每百万输入/缓存输入/输出分别 ¥0.2/0.04/0.8，未改本机已冻结配置；估算不是供应商账单。

1.4 Compiler 输入 2165、输出 203 tokens，2667ms；Audit 输入 4460、输出 2411 tokens，24232ms。1.3 Compiler 输入 1994、输出 523 tokens，5630ms。

1.4 的原质量检查已再次只读复核：两种真人选角都能执行找钥匙→开柜，私有奖励只授予乙，甲须等乙分享；动作需公开钥匙，而材料不被增加额外公开前置；固定结尾保持。这里的“两角色完成”是确定性规则验证，未额外调用角色对白模型，也不代表商业本整局通过。

Audit 未保存为成功报告；收据仅定位第二条发现的 sources 字段格式不合格。被拒绝的原始回答没有持久化，不能推断它究竟为空、类型错误或其他具体值，更不能擅自补引用使其通过。本轮停止追加付费请求。后续先解决 Audit 输出的来源结构，并保持候选来源绑定和既有失败记录。

## 证据与离线检查

仓库外 `private-data/import-jobs/frozen-rule-plan-smoke/`：

- `20260906T080103Z-zwm69jdf`：1.3 零调用预览。
- `20260906T080117Z-2615vo_9`：1.3 真实失败任务。
- `20260906T080814Z-k7zaj9xm`：1.4 零调用预览。
- `20260906T080832Z-c9wllc_k`：1.4 编译通过、Audit 格式失败任务。
- `20260906-root-review.json`：原始文件摘要、规则/正文逐项比较、独立质量复核和合计费用；原 SQLite 均只读复核。

后端专用离线入口 **1672 passed**，363 条存量弃用警告，18.54 秒（本轮新增 20 项已包含）；前端八套 **174 passed**（新增 2 项已包含）。完整 TypeScript、修改文件 lint、26 页生产构建通过，API 地址为 `http://127.0.0.1:8010`。开发预检退出 0，自测 16 项通过。未更改数据库结构、未运行主 PostgreSQL 迁移或新 PostgreSQL 集成测试；本轮未启动浏览器/本地应用服务，4F 浏览器证据仅保留其原范围。

显式测试入口：`backend/scripts/real_frozen_rule_plan_smoke.py`；默认只预览，`--confirm-frozen-text` 选择 1.4。真实运行还需 `--execute --confirm-paid TWO_SYNTHETIC_FROZEN_RULE_PLAN_CALLS` 以及已授权的本机付费开关。工具会写入新的私有目录、绑定输入/配置/提示与输出摘要，并在发送前核实费用已持久预占。不得为了消除失败状态直接重复运行历史任务。
