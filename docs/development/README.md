# VS Code 开发交接

2026-09-08 图片线索与内容修复交付：开场地图/等待警方情境补齐，24张遗漏卡面授权追加；180材料同类扫描、28条材料经原图修正，截图“了、”恢复7。独立“我收藏的线索”及Dock/短屏避让完成。前端372、后端及私有导出各2613、预检16、类型/lint/webpack及独立复核通过，真实页面图片/收藏/刷新/取消/800短屏与390px交互通过；截图接口超时，使用DOM几何证据。原9开场/7试玩/443事件及全部旧行保持，仅追加新候选/导入/本机开发release；新模型/ASR0、费用¥0。构建`KoGwlXTQzmdgCcl2S3qEV`，后端GitHub `m3-20260908.7` / `453c8b1`已核验。停在M3用户验证，不进入M4/正式发布；原模型语义与真实ASR待验保留。详见[线索与有源修复](M3_CLUE_REPAIR.md)。下一轮M3按反馈修复复验，建议极高。下文保留历史检查点。
[最新开场核查与顶部操作栏](M3_OPENING_PREVIEW.md)：开场顶部常驻、右侧开始游戏动效已交付；前端353、后端2589与类型/lint/webpack/预检16通过。已确认当前开场无后期私密材料，但包含本版编辑玩法/五封面安排，遗漏等待警方情境与地图原图，不能标内容齐全。冻结包与后端未改，7表及本轮443旧事件不变、真实调用0。下一轮仍M3，按来源补齐新候选与开场图片权限后复验。

[此前顶部导航交付与十项验收标准](M3_TOP_NAVIGATION.md)：剧本信息/阶段/入口已合并吸附顶部，任务/回忆/手记原地查阅，短屏输入与错误避让；本机页面已更新，7表及441事件不变。本轮前端352、后端2589、类型/lint/webpack/预检16通过，模型/ASR调用0。真实手机软键盘、窗口快速缩放与既有AI内容问题仍需人工复验。私有后端PRD版本 `m3-20260908.6` / `48cb179` 已同步，后端运行代码未改、专用仓库不含前端。M3继续等待用户验证，M4未开始。

[此前 PRD 登记与检查记录](PRD_PROGRESS_REGISTER.md)：私有后端版本 `m3-20260908.5` / `98685c3` 已同步，包含更新 PRD 与旧稿归档，运行代码未改。

2026-09-08 当前产品总览：[简版 PRD](../../backend/PRD.MD)，包含开发大纲、M1—M4 进度、验收流程、API 配置及 LiteLLM 登记。旧 PRD 已归档；本页后续大量“当前/下一轮”文字是历史交接，阅读时以该总览和最新专题记录为准。

最新范围：M3 唐小姐文字整局待用户验收，排版、随时查阅和互动反馈已迭代；[豆包 ASR 输入](M3_DOUBAO_SPEECH_INPUT.md)代码/模拟验证完成，缺语音专用凭据，真实识别尚未验证。TTS 仍在后续版本，M4 未开始。[LiteLLM](LITELLM_INTEGRATION_ASSESSMENT.md)仅登记为候选，未安装/接入；文字 API 不需重新配置。本轮更新前端与文档，没有新的模型调用或后端服务配置变更。

2026-09-08 最新交付：M3 已到可供用户完整验证的阶段，固定唐小姐与四 AI；本机入口 http://127.0.0.1:18032，持久启动器和详细验证流程已交付。最终真实整局 173 次、29 调查、五席封卷结算、15 个实际恢复点一致；新局页面完成真实公开/私聊、过期登录返回及重启恢复。后端 2503 项、最终前端 238 项、类型/预检/16 自测、webpack 构建通过；lint 无错误、1 条导航建议警告。内容仍有必讲背景遗漏、确定性升级、引用失准及电话重复，另保留 1 条页面原卡复制拦截，不能标全部语义通过。持续开发费用估算 ¥5.12414840，全部 M3 ¥5.26228880。此处停止自动开发，等待用户按流程验证与反馈；之后先修复 M3 反馈并复验，再进入 M4。建议开发思考强度极高（权限、事件、内容与费用需一起核对）；runtime_ready=false、未正式发布。详见 [M3 持续开发与完整验证](M3_CONTINUOUS_VALIDATION.md)。

下文保留历次检查点；其中旧的“最新”“尚未授权”“整局待做”等表述均为当时状态，以本段交付状态和持续开发记录为准。

2026-09-08 最新：用户确认此次测试金额并继续开发。已恢复新引用格式四例的现存真实执行并完成独立复核：4 次请求、35914 tokens、估算 ¥0.0459048（上限 ¥0.50），预占归零；本次续接新增真实调用 0。格式 4/4 通过，J/S 所测内容通过，H 有源前提通过但反问有效性有限；Y 的记忆断言依据不足，药物不确定性与未知短醒时间两项未覆盖，整批语义未通过。新增禁网录制核验和普通/优化各 10 项检查、四个定向追问离线接线；相关后端 33 项、预检及 16 自测通过，两项独立只读复核完成。38 个本批及 581 个旧文件摘要未变；三批 M3 累计 13 次、108429 tokens、估算 ¥0.1381404。后续追问仅为离线案例，未获新付费批次授权；不重复收费、不安装 LiteLLM。M3 整局、M4 与正式发布仍未通过，runtime_ready=false。下一步 M3：定向验证未覆盖边界与电话回应有效性，再准备完整整局；建议开发思考强度极高。 [本次内容复核与离线开发](M3_CATALOG_REVIEW.md)。

2026-09-08 此前准备检查点：用户决定不安装、不接入 LiteLLM，继续 M3。已完成新引用格式四例复验准备：套件 c6e17f38…dc8ae6，显式对白1.5/电话1.2，原四例正文/CLAIM/判据保持，独立新请求身份；四例免费演练、执行器普通及优化模式各13项、相关后端33项、预检及16自测通过。输入/来源与执行器两项独立只读复核无阻塞，581个旧文件摘要未变。新真实调用0、费用0、尚无新批准或执行凭据；下一步提交最多4次/40万tokens/估算¥0.50的新批次批准后进行实际格式与内容核对，原两批继续关闭。不自动追加付费批次、不启动整局或改变发布门禁；M3未完成，建议开发极高。详见[新引用格式验收](M3_CATALOG_ACCEPTANCE.md)。

此前检查点：M1三项采用已落实，M3工程已接线但真实整局未通过。最新4例复测已获用户“好的”批准，实际第2例引用类别错误INVALID后停止：1通过、1失败、2未测；2次请求、17085 tokens、保守¥0.0215724，预占归零。首例经历/必讲通过，第二例本次保密和归属正确但错误引用不能人工修正后算通过。两批M3累计9次请求、估算¥0.0922356。已完成新显式对白1.5/电话1.2引用目录约束：当次合法(collection,id)成对选择，排除策略/裁掉的历史，计入输入预算；旧提示/协议/prepared及失败收据保留。后端2382项通过，新引用16项覆盖；新候选124个输入容量检查通过，最大50952字节。候选51d31fa…及323来源、186步纯规则整局、18评分反例保持。前端本轮未改，旧分版本页面证据不冒充新模型验收。下一步仍为M3新引用格式真实复验及未测电话/不确定性，再回到整局；本批已结束，未启动新付费批次。后台审核、M4、正式发布均未通过。详见[M3整局记录](M3_FULL_TEXT_PLAY.md)；runtime_ready=false，建议开发极高。

M2 已完成约定的有来源首阶段互动片段，见[角色对白与M2验收](M2_ROLE_DIALOGUE.md)：真人发言、四AI回应与追问、实际台词触发私密回忆、保存恢复和明确元信息请求的程序拒答。后端2096、前端209、类型/lint/26页构建、隔离浏览器及独立复核通过。真实20次估算¥0.03355456；1.0内容12/13、1.1内容4/7，原失败保留。最终1.2仅做20输入离线复验和页面录制重放，没有新增真实生成。模型仍可能漏讲必讲内容，内部仅记ATTEMPTED_UNVERIFIED，不计分。M3整局、M4五选角、1.3模型审核及正式发布未完成，runtime_ready=false。下一轮M3准备阶段/封卷/答卷/评分的有源整局链和具体待决规则方案；建议极高，复杂权限与评分终审可用Ultra。

当前主线按用户 2026-09-07 确认的[交付计划](DELIVERY_PLAN.md)推进：第一版是《孽岛疑云》完整文字试玩，M1 规则与来源对照 → M2 一段真实角色互动 → M3 完整文字整局 → M4 五角色及用户试玩；随后语音体验与上线。后台审核仍未真实验收通过，保持支线，不默认追加提示版本。计划批准不等于编辑规则或发布批准。M2有限片段已完成，M1其余规则决定与M3整局继续推进，最新证据见上段。

此前 M2 子项见[实际事件触发私密回忆](M2_MEMORY_TRIGGERS.md)：显式 1.3 已将实际公开发言和新获线索接入私密回忆授予、角色隔离、事件重放及本人页面。后端 2048 项、前端 204 项、类型/lint/26 页构建、隔离浏览器与真实单卡引擎核对通过；浏览器使用虚构双角色和模拟 SDK，不能代替商业整段。真实请求 0、费用 ¥0，临时服务已停止。新 1.3 模型编译/审核尚未接入，正式发布门禁继续阻断未审包；自然对白、转述义务完成、投票评分尚未完成。下一轮接一个首阶段片段的角色转述及追问回复，建议开发极高，runtime_ready=false。

此前 M2 见[调查建议真实验收](M2_PROPOSAL_ACCEPTANCE.md)：五角色31目标25回忆已有来源对照；原调查建议适配器双模型各8次实测，豆包完整案例7/8、千问5/8，仍有引用不相关和一次无效输出，不能宣称角色质量通过。合计16次估算¥0.00746476，本批停止新增。后端2018项、预检与独立复核通过，未修改前端或迁移主库。下一轮接实际发言触发私密回忆，再推进自然转述，runtime_ready=false。

此前 M2 子项见[角色决策能力评估](ROLE_DECISION_ASSESSMENT.md)：已评估两家固定模型各六个虚构调查场景，非思考模式下基础选择符合预期；台词仍有无依据表述/理由生硬，不宣称角色质量通过。新增纯视图/决策提议与独立评估工具，后端 1909 项通过；实际 12 次估算 ¥0.00459568，无重试。本批停止追加请求，继续角色信息与决策接线，尚无新玩家页面/集体表决/回忆功能，runtime_ready=false。

此前 [4N 引用目录任务接线](PHASE_4_CITATION_AUTHORING.md)：显式 Authoring 1.12 已将引用目录接入请求、动态 schema/预算、持久账本、worker 和工作台。旧任务保留原版本；跨目标/越界引用拒绝，未知调用不重发，BLOCKER 不删除。后端 1893 项、前端 188 项、类型/lint/26 页构建通过；四份旧冻结样本的新契约核对通过，旧 1.11 请求逐项未变。真实模型调用 0、新增费用 ¥0；供应商兼容性和语义准确性仍未验证，runtime_ready=false。下一步准备独立受限格式与语义验收，无需用户先改配置。

此前 [4M 引用目录审核离线原型](PHASE_4_CITATION_PROTOTYPE.md)：新增只选择已有引用的候选格式，服务器确定目标和来源，跨目标/越界/重复索引拒绝，保留 BLOCKER 及原完整领域校验。四份旧冻结样本共 60 项引用只读验证通过，后端 1869 项、前端 186 项通过；零模型调用、零新增费用。仅完成纯本地原型，尚未接入任务/worker/工作台，现有流程仍为 1.11，供应商兼容性与真实审核准确性未验证。下一步以显式新版本接入动态目录/schema 的快照、字节预算及持久核对，不直接替换旧任务；runtime_ready=false。

此前 [4L 固定样本真实审核验收](PHASE_4_SEMANTIC_ACCEPTANCE.md)：完成四组虚构 Audit 的冻结输入、单次执行凭据、预算及失败账本。真实请求均正常 stop，但 4/4 被格式或来源编号校验拒绝，语义结果 NOT_EVALUABLE，不能宣称误报改善。4 次请求估算 ¥0.0058458、预占 ¥0.0302446；停止本轮付费调用。后续补上目标不存在/来源编号不可用的安全位置诊断，仅离线与模拟验证、不回填旧收据。后端 1844 项、前端 186 项、类型/lint/26 页构建通过；无人审批准发布，runtime_ready=false。下一步先离线核对目标目录与输出约束，不靠重发、补引用或删发现制造通过。

此前 [4K 版本化审核运行规则与反例](PHASE_4_RUNTIME_AUDIT.md)：显式 1.11 将程序规则固定在审核系统消息，并复制当前候选结尾引用供核对；来源不能覆盖规则，旧任务不升级。新增正常、点数无法用尽、角色权限卡关三组离线样本，同时检查误报与漏报。后端 1824 项、前端 185 项、类型/lint/26 页构建通过，零真实模型调用、零新增费用；尚未证明真实审核准确率改善，无人审批准发布，runtime_ready=false。下一步冻结独立留出样本和评价规则后进行有边界的真实语义验收，不能仅以少报问题视为成功。

此前 [4J 审核格式约束与程序规则核对](PHASE_4_STRICT_AUDIT.md)：修复顶层 schema 并接入供应商结构约束，显式新任务 1.10 保留全部本地校验。真实 Audit 已能正常读取并通过来源编号校验，但 3 项意见含 1 BLOCKER，存在事实误报，烟测仍 FAILED；任务 COMPLETED 不代表人审通过。新增只读规则事实和实际结尾真相编号对照。后端 1800 项、前端 184 项、类型/lint/26 页构建通过。四轮共 8 次真实请求估算 ¥0.0102844，累计预占 ¥0.0616020；停止本轮付费调用，未批准发布，runtime_ready=false。下一步准备版本化审核运行上下文及独立语义验收集，不要求用户先换模型或改配置。

此前 [4I 简短审核与安全诊断](PHASE_4_BOUNDED_AUDIT.md)：新增显式 1.6 的长度限制与完整性标记，未完成报告不接收。真实 Compiler 及固定小样本质量通过；Audit 本次正常结束、输出 341 tokens，但格式仍不合格，任务 BLOCKED。后续补上安全字段诊断，未追加付费验证。后端 1743 项、前端 178 项、类型/lint/26 页构建通过；2 次真实请求估算 ¥0.0017700，无批准发布，runtime_ready=false。

此前 [4H 审核来源编号](PHASE_4_INDEXED_AUDIT.md)：新增显式 1.5，让 AI 选择目标自身的来源编号，服务端严格还原所选引用。后端 1700 项、前端 175 项及 26 页构建通过。真实 Compiler、来源核验和固定小样本质量通过；Audit 非正常结束，输出达到 4096 tokens，任务仍 BLOCKED，来源编号尚未通过真实验收。2 次真实请求估算 ¥0.0047386，未批准发布，runtime_ready=false。下一步处理审核长度与完整性，本轮不追加付费调用。

此前 [4G 固定正文与真实模型验收](PHASE_4_FROZEN_TEXT_ACCEPTANCE.md)：1.4 真实编译、来源与固定小样本质量检查通过，Audit 的来源字段格式失败，整链仍未通过。已固定正文、防止模型夹带管理说明；后端 1672 项、前端 174 项和 26 页构建通过。本轮 3 次真实请求合计估算 ¥0.0042334，停止追加付费请求。

此前工作包为[阶段 4F：冻结规则草案与原文摘录](PHASE_4_FROZEN_RULE_PLAN.md)：任务固定准备人员提交的待审规则草案，模型只能填写指定原文槽位；新接口、工作台、版本隔离和 worker 已接线。后端 1652 项、前端 172 项和隔离浏览器检查通过，本轮零真实模型调用；草案不是人审批准，新流程真实模型验收仍待做。此前 [4E 规则与来源逐项核对](PHASE_4_RULE_REVIEW.md) 保留。用户已授权范围内的开发持续进行；只有需要其补充真实资料或决定具体业务规则时才集中说明，不要求每个工程步骤再次确认。

4D 已把 `script-package/1.2` 的每阶段共享调查点、固定成本、动作前置、授权奖励及结束门槛接入 `/play/package-play`。仍从固定开场显式建局，1.2 不支持旧 4A 演练；旧包及历史事件哈希不变。新 Authoring/Audit 版本、动作/阶段预算审核目标、人工确认与独立发布已工程接通，调查点与 AI 费用独立。离线及隔离浏览器全链通过，前端 163 项、隔离 PostgreSQL 11 项、26 页构建通过；浏览器的 2 次编译/审核和 1 次角色调用均为模拟 SDK，未批准商业候选。

另行三次真实 Compiler v5/v6/v7 通过通用结构与原文摘录校验后，均被固定合成烟测的精确材料前置预期检查拦截（`SMOKE_MATERIAL_PREREQUISITE_MISMATCH`），Audit 未调用，本轮付费提示迭代已停止。此问题是新 1.2 内容映射未达预期，不是角色渠道配置错误，也不证明通用校验器能自动识别任意原文语义。新 1.2 真实整链仍未通过；自然对白、完整票制评分、主持与语音未完成，`runtime_ready=false`。以下保留旧 1.1、4B、4C 等历史证据。

更新：2026-09-06。此前[阶段 4B 角色材料问答与结尾揭晓](PHASE_4_TEXT_PLAY.md)已代码接线。新入口 `/play/package-play` 从固定开场显式创建独立会话，重新核验当前发布并固定角色、版本、模型和本局预算；旧开场与 4A 演练不升级。各 AI 角色共用本局公开记录和预算，输入仍按各自权限隔离。模型仅选择可公开资料引用，服务端核验后原样组装回答；选中的 AI 本人 MAY_SHARE/MUST_SHARE 材料正式公开并按规则继续解锁，KEEP_PRIVATE 不进入模型输入。真人仍手动推进和分享；到末阶段显式 SETTLE 只揭晓包指定的结尾说明与 truth_ids，之后禁止新操作。

后续 4C 已通过一次真实火山模型的虚构材料选择→公开解锁→指定结尾→重读，估算费用 ¥0.000616；真实本机 PostgreSQL 随机隔离 schema 中 j0→o5 迁移及并发/恢复 9 项通过，临时 schema 已核验清理，未迁移主 schema。4C 当时后端离线检查 1443 passed（363 条存量警告，12.41 秒）。这些结果不代表商业本、自然对白或真实人审发布通过；`runtime_ready=false`，商业本胜负规则、自然对白、主持和语音仍待接线。4C 同时复核了《孽岛疑云》来源与编辑规则差异，详见阶段 4C 记录。协议见[文字试玩接口](../contracts/package-play.md)。

4B 历史验收状态：后端 1378 passed（363 条已有弃用警告，12.35 秒；新增 183 项已计入），前端八套 152 passed，完整 TypeScript 无诊断，CommonJS 测试文件类型配置下的修改文件 lint 通过，预检退出码 0、自测 16 项通过。隔离合成夹具浏览器通过从旧开场显式创建、AI 材料原文问答与正式分享、真人分享后条件解锁、三阶段推进、指定真相揭晓、刷新/重进、权限与 390px 无溢出验收；最终 1 份试玩、6 条事件、1 次模拟 SDK 调用、0 次真实模型调用，旧开场保持不变，浏览器空间 60 已关闭，临时 API 18017/前端 13017 已正常退出且端口关闭。4B 当时最终生产构建通过，共 26 页，生产 API 配置仍为 `http://127.0.0.1:8010`。

此前[阶段 4A 固定角色阶段演练](PHASE_4_RULES_PREVIEW.md)已实现：从已有开场显式创建独立演练，重新核验当前发布并固定角色、版本；旧开场仍只读，不自动升级。`/play/package-flow` 支持手动顺序推进，材料按阶段下限和全部公开证据条件解锁，原生公开证据连续解锁；本人已解锁的 MAY_SHARE/MUST_SHARE 材料可手动公开，KEEP_PRIVATE 禁止公开。MUST_SHARE 不自动执行，也不额外创造主持截止或推进条件。

4A 发布失效后，新动作冻结，GET 和原动作幂等重放只保留已获内容。该演练仍为 `RULES_PREVIEW`、`runtime_ready=false`，不提供 AI 角色、主持、投票、结算真相或完整整局，也不套用旧两轮搜证。新增两表迁移 `n4a5b6c7d8e9` 仅离线验证，未应用真实 PostgreSQL。请求 schema、API 与边界见[阶段演练契约](../contracts/package-flow.md)。

4A 最终后端安全入口 1195 passed（363 条已有弃用警告，10.71 秒），新增 110 项包含规则 53、持久化 37、整合 20，均已计入总数；前端 114 passed（新增 25、存量 89），完整 TypeScript 与修改文件 lint 无诊断，预检退出码 0、自测 16 项通过，最新代码的 25 页生产构建通过。隔离合成夹具浏览器通过显式创建、分享解锁、阶段推进、刷新与重进恢复、权限及 390px 无溢出验收；最终 1 份演练、3 次动作，新增模型调用为 0。旧开场仍为只读预览，临时 API 18016/前端 13016 已关闭，生产 API 配置仍为 8010。

此前[第三阶段 D 人审发布与版本绑定](PHASE_3_PUBLICATION_BINDING.md)实现了全量人工审核依据门禁、模型意见逐项人工处理、确认与登记发布分离，以及固定角色开场；旧直接 PUBLISHED 路径已阻断。该阶段本身不提供完整规则与 AI 整局。

已发布候选收到后续审核变更后，不能再新建开场，必须新 `content_version` 重新审核；已有开场与旧游戏会话保留原版本。3D 该轮没有付费模型调用、商业正文处理或对真实候选的实际批准，新增 `m3f4a5b6c7d8` 迁移仅离线验证，未应用真实 PostgreSQL。当前开发检查不要求用户先做人工试玩。

3D 最终后端安全入口 1085 passed（363 条存量弃用警告，9.70 秒），前端 89 passed，完整 TypeScript 与修改文件 lint 通过且无诊断，24 页生产构建通过；开发预检退出码 0、自测 16 项通过。14 项迁移、锁与 HTTP 整合测试已包含在 1085 项中，不重复计数。隔离合成夹具浏览器验收完成：管理员显式保存 1 份确认、独立登记 1 份发布，普通玩家选择角色创建 1 份开场，两次刷新保留发布版本及本角色材料。公共证据与本人材料可见，其他私本、真相、结算与来源路径不可见；开场读取的本人 200、其他用户 404 响应均为 no-store，匿名 401 与玩家访问管理员入口 403 由认证依赖直接拒绝，未设置该响应头。管理员与玩家页面均在 390px 无横向溢出，新增模型尝试为 0。

3D 临时 API 18015、前端 13015 已正常退出且端口关闭，浏览器任务空间 58 已完成并关闭；该轮原 PostgreSQL 55432 与 Redis 56379 保持运行，生产构建的 API 地址为 `http://127.0.0.1:8010`。

[第三阶段 C 规则契约与真实验收](PHASE_3_RULE_CONTRACT_REVIEW.md)保留此前小合成实测：双角色、双阶段的 Compiler v4 → 来源核验 → 候选 → Audit v3 链路完成，质量标准 1.1 通过，Audit 五维完整、2 项发现、0 BLOCKER。该轮首项编译通过但 Audit 输出无效，以及更早三轮 Compiler 失败均保留；该轮两项估算费用共 ¥0.0061908、累计预占 ¥0.0323508，五项历史任务合计估算 ¥0.0117958。3C 当时后端 1005 passed（363 条存量警告）、前端 62 passed，TypeScript 和 23 页生产构建通过；真实任务结果经隔离只读 API、模拟管理员完成桌面及 390px 浏览器查看/刷新，无额外模型调用，数据库与收据哈希未变。该证据不代表商业本、模型整体语义质量、生产环境或完整产品验收，也不授予真实候选发布批准。
本页是工程入口，产品大纲维护在用户本轮要求整理的[现有 PRD](../../backend/PRD.MD)，不更换基座。

给第一次开发的产品经理：现在可以继续开发，用户已在 2026-09-04 明确授权并打开“真实付费”开关。默认角色对白走火山方舟 Character，百炼 Qwen 在角色对白场景是手动备用；后台 Compiler/Audit 使用已适配的百炼固定 Qwen。两家角色渠道都配好也不会自动同时调用，同一局只用一家。最简单的本地操作见[小白本地启动说明](LOCAL_DEVELOPMENT.md)。

## 先读什么

先看[当前简版 PRD](../../backend/PRD.MD)和[交付计划](DELIVERY_PLAN.md)，再按任务阅读以下工程依据：

1. [AGENTS.md](../../AGENTS.md)：用户已确认的方向、边界和安全命令。
2. [技术适配声明](TECHNICAL_ADAPTATION.md)：为什么保留当前架构、第一阶段做什么以及哪些决定留到真实验收。
3. [第一阶段技术文档](PHASE_1_MODEL_AND_BUDGET.md)：模型、思考控制、费用预占和验收范围。
4. [第一阶段离线验证记录](PHASE_1_OFFLINE_VERIFICATION.md)：无网络自动化命令与结果。
5. [PostgreSQL 预算并发记录](POSTGRES_BUDGET_INTEGRATION.md)、[真实模型直连记录](REAL_API_SMOKE.md)与[核心服务纵向记录](LIVE_VERTICAL_SMOKE.md)：三类独立实测证据，不能互相替代。
6. [Blueprint 映射](BLUEPRINT_MAPPING.md)：借鉴哪些思想，哪些仍未实现。
7. [角色运行契约](../contracts/agent-runtime.md)、[导入契约](../contracts/script-import.md)、[阶段演练契约](../contracts/package-flow.md)与[文字试玩契约](../contracts/package-play.md)：区分完整运行目标与已接入的候选、审核、发布、开场、演练和受限角色问答。
8. [验收清单](ACCEPTANCE.md)：以可复现结果区分“写完”“可启动”“可完整游玩”。
9. [已完成的基座升级](../../backend/docs/FUSION_BASE_UPGRADE.md)：此前实际实现及能力边界。

## 工作目录和环境

- 当前主仓库：桌面“AI人生海海剧本杀”中的 `ai-jubensha-fusion`。
- Python 要求 `>=3.13,<3.14`；已有 `backend/.venv` 可执行 Python。
- 版本以锁文件为准：本次核实 Next 16.3.3、React 19.2.8、TypeScript 5.8.3。
- 已有 `frontend/node_modules`，本次保留依赖，无需为整理目录重装。
- VS Code 应用存在；`code` 未在 PATH 不妨碍通过“文件 → 打开工作区”打开。
- 推荐 Python/Pylance 扩展；项目虚拟环境已安装 debugpy，并提供只绑定本机的 VS Code 调试配置。
- 不执行 agent-blueprint 的 setup.sh，不使用其 Python3.10/Anthropic 示例替换现有项目。

迁移过的虚拟环境中，部分可执行脚本 shebang 指向旧路径。
使用 `backend/.venv/bin/python` 或 `python -m 模块`，不要直接调用 `.venv/bin/pytest`。
彻底重建环境是后续独立操作，须先保留锁文件并确认网络和磁盘，不在本次整理中自动执行。

## 可以直接运行的离线检查

下列命令从仓库根目录运行，不启动应用、不连接 PostgreSQL、不使用真实模型：

```sh
backend/.venv/bin/python scripts/dev_preflight.py
backend/.venv/bin/python -m unittest discover -s scripts -p 'test_dev_preflight.py'
backend/.venv/bin/python backend/scripts/test_fusion_security.py
```

前端检查从 `frontend` 目录运行：

```sh
node --test tests/fusion-evidence-panel.test.cjs tests/source-bundle-panel.test.cjs tests/script-review-panel.test.cjs tests/authoring-jobs-panel.test.cjs tests/script-publication-panel.test.cjs tests/package-preview-service.test.cjs tests/package-flow-panel.test.cjs tests/package-play-panel.test.cjs
node node_modules/typescript/bin/tsc --noEmit --incremental false
```

VS Code 的“终端 → 运行任务”提供同等任务。不要自动发现并运行旧全量 pytest：
预检退出码0=通过，1=编码前置阻塞，2=可编码但有准备/集成警告；预检与默认离线测试分开运行。
旧 `tests/conftest.py` 会初始化应用，安全测试必须走上述隔离入口。
前端 `npm run lint` 与 `npm run check` 已迁移到 ESLint 9 flat config；它们仍是离线代码检查，不会调用模型。
不要自动运行 `generate-api`：它依赖正在运行的真实后端并会重写生成客户端。

## 启动服务之前

本机未安装 Docker，不能沿用历史“容器仍运行”的判断；当前改用项目专用 PostgreSQL 与 Redis 完成本地开发，生产 Docker/托管环境仍须另验。
9月1日 PostgreSQL dump 和 globals SQL 已保留，但不代表覆盖最新数据或能恢复。
另有一套仅供本项目隔离测试的 Postgres.app 2.9.6（PostgreSQL 17.11），固定测试目标为 `fusion_it@127.0.0.1:55432/fusion_pg_it_sandbox`；它不是历史数据库、备份恢复结果或生产运行环境。

真正启动前先完成以下步骤：

1. 确认采用哪套 Docker/数据库运行环境；如果有旧数据库，先识别实际卷与最新数据。
2. 验证备份可恢复到独立测试库，不能恢复覆盖原库。
3. `docker-compose.yml` 的项目名是 `ai-jubensha-fusion`，`docker-compose.dev.yml` 是 `jubensha-dev`。
   两者使用不同卷；改工作目录不等于数据库已迁移。不要通过切换 Compose 文件判断数据“丢失”。
4. 核对数据库主机、端口、库名及用途；开发库必须与需保留的库区分。
5. 隔离数据库和虚构资料真实模型技术验证已有独立记录；正常服务与页面仍须先用虚构测试局验收。商业素材虽已获准交给所选官方云模型，但整局使用仍须等版本化知识与阶段回忆边界完成。

已有根 `.env` 的两把密钥值没有被回显或改动，权限保持600；本轮只补入已确认的非秘密 provider、固定模型版本和预算费率。**不能复制模板覆盖它。**
根 `.env.example` 是容器模板，不是宿主机开发配置；示例模型名更新并不改变实际密钥或运行环境。

| 配置 | 容器模板 | 宿主机开发需要核对 |
|---|---|---|
| DB_HOST | db | localhost/127.0.0.1，必须确定对应哪个库 |
| REDIS_URL | redis://redis:6379/0 | 本机已使用 redis://127.0.0.1:56379/0；生产仍使用独立服务 |
| NEXT_PUBLIC_API_URL | / | http://localhost:8010，避免前端3001误当同源后端 |
| CORS_ORIGINS | 生产站点占位 | http://localhost:3001 与实际开发地址一致 |
| ENABLE_MEDIA_FEATURES | false | 暂保留false；不代表未来语音已完成 |
| FUSION_PLAYER_PROVIDER | volcengine_ark | 默认角色对白走火山方舟；切换百炼只对新局显式生效，不自动 fallback |
| ARK_CHARACTER_MODEL | doubao-seed-character-260628 | 默认角色对白模型；Key、地域和账户模型权限须匹配 |
| DASHSCOPE_QWEN_MODEL | qwen3.7-flash-2026-07-15 | 固定版手动备用／未来 GM 总结候选；当前 GM 链路未实现 |
| ENABLE_PAID_MODEL_CALLS | false | 本机受保护 `.env` 已按用户授权设为 true；容器模板仍安全默认 false。只有实际模型请求才扣费 |
| GAME_TOKEN_BUDGET | 3000000 | 标准局技术兜底；缺失环境变量时代码仍使用更小的失败关闭默认值 |
| GAME_COST_BUDGET_CNY | 10 | Fusion 文字模型单局硬上限；运营目标为不超过 ¥7 |
| Ark Character ≤32K 费率 | 输入 0.80／缓存 0.16／输出 2.00 | 人民币／百万 Token，2026-09-04 北京地域；启用当天复核 |
| 百炼 Qwen ≤32K 费率 | 输入 0.20／缓存 0.04／输出 0.80 | 人民币／百万 Token，2026-09-04 北京地域；启用当天复核 |
| 角色对白思考模式 | 代码固定 disabled | 当前不能通过环境变量打开；未来 GM 思考须另建契约和预算 |

VS Code 提供的服务启动任务仅可手动选择，它们会加载环境和初始化应用；**不是离线预览**。
启动后端可能建表；执行游戏动作可能发出付费请求。阅读任务提示并核对数据库目标后才启动。
本地任务只绑定回环地址；生产部署使用独立流程。不要清卷、强制迁移或自动发布。

`ENABLE_PAID_MODEL_CALLS` 保护正常的 Fusion 玩家角色对白与新 4B 问答的预算策略，不保护旧剧本生成、编辑、媒体、未来 GM 或专用真实冒烟 CLI。冒烟 CLI 另有必填确认词、单次请求和 ¥0.01 本地估算上限；没有新的明确授权不要再次运行。统一出网门禁完成前，不要通过旧入口发送商业正文。

## 下一阶段从哪里开始

| 顺序 | 工作包 | 完成证据 |
|---|---|---|
| 0 | 审查“待合并开发副本”的独有schema/migration/tests | [已审查](ARCHIVED_COPY_REVIEW.md)；创作蓝图不直接合并，保留历史副本和当前安全改动 |
| 1 | 模型适配、思考控制、用量/费用预占 | 离线 359 passed；真实 PostgreSQL 双连接预算锁、迁移链、基础 HTTP/登录均通过；百炼与默认火山核心纵向链通过；恢复场景、建局动作和人工口吻待验收 |
| 2 | 版本化剧本包、确定性校验器、导入任务记录 | [已实现候选接收](PHASE_2_PACKAGE_INTAKE.md)，当时 103 项新增离线测试通过；非法包 BLOCKED，合法包只存候选；来源核验和开场版本接线分别见 3A、3D，真实库迁移待完成 |
| 3A | 来源冻结 / 核验 / 管理员预览 | [已实现](PHASE_3_SOURCE_VERIFICATION.md)，私有快照、来源定位核验与报告；实际文件一致，不等于语义/人工审核通过 |
| 3B | 来源契约 / 人工审核记录 | [已实现](PHASE_3_REVIEW_RECORDS.md)：v1.1 归属、报告与处理历史绑定具体包；尚未批准发布 |
| 3C | Compiler / 自动 Audit 任务 | [旧 1.1 双角色双阶段合成资料真实链已通过](PHASE_3_RULE_CONTRACT_REVIEW.md)；真实结果经隔离只读 API/模拟管理员完成浏览器验收；后台显式 worker、查询/取消/恢复、模型建议，不批准发布 |
| 3D | 人审发布门禁 / 固定版本开场预览 | [已实现](PHASE_3_PUBLICATION_BINDING.md)：全量依据与逐项人工决定绑定确认，独立登记不可变发布；只读开场固定角色/版本，完整运行时未接入；真实 PostgreSQL 迁移未应用 |
| 4A | 固定角色确定性阶段演练 | [已实现](PHASE_4_RULES_PREVIEW.md)：显式新建、顺序推进、阶段下限与公开证据条件解锁、自有材料手动分享、追加历史与版本冲突；无 AI/主持/整局 |
| 4B | 角色材料问答 / 末阶段指定结尾 | [离线及隔离浏览器已通过](PHASE_4_TEXT_PLAY.md)：独立局、角色隔离、共用预算、引用选择后正式分享与原文回答，末阶段显式揭晓；后续真实小样本见 4C，完整商业玩法未验 |
| 4C | 真实角色材料选择 / 隔离数据库恢复 | [虚构资料真实角色链与 9 项 PostgreSQL 验收通过](PHASE_4_REAL_INTEGRATION.md)；不代表商业本或自然对白完成 |
| 4D | 1.2 共享调查点 / 新审核发布链 | [工程、隔离浏览器及 PostgreSQL 11 项通过](PHASE_4_BUDGETED_INVESTIGATION.md)；三次真实新版 Compiler 未满足固定合成烟测的精确前置预期，Audit 未调用，不能宣称新 1.2 真实整链通过 |
| 4 后续 | 孽岛专属规则、角色自知/回忆、AI 分享/解释/提示/主持结算 | 通用演练不能证明商业本规则已表达完整；按确认的原本规则逐条接线与验收，不靠 LLM 临时改规则 |
| 5 | 文字整局与网络故障恢复 | 完整回放、重复动作不重复落库、事件不乱序、角色不越权 |
| 6 | TTS / ASR / 播放打断 | 只播放审查通过台词；记录首音和端到端延迟；过期音频丢弃 |

默认任务标签为“测试：全部离线检查”。服务启动任务须在专用终端输入START才继续；
后端任务会先启动本项目专用的 PostgreSQL 和 Redis，再显式使用127.0.0.1:55432与127.0.0.1:56379并关闭媒体，库名/用户/密码仍来自受保护的根 `.env`。
前端统一使用127.0.0.1:3001并连接127.0.0.1:8010，避免 localhost/同源配置混用。

当前不需要为“准备齐全”先堆更多框架或模型。提供商和单局费用方案已经确定；每次新增真实 API 测试前仍须复核当天价格、确认独立测试局并取得明确扣费授权。默认火山已经用虚构资料通过当前核心技术链，但仍不能在失败时自动切到百炼；最终 TTS 音色不阻塞前面的开发。

## 文件与记忆规则

私本/OCR/导入产物/审计全文在仓库父目录的私有资料中，不进入 Git、公开 issue 或默认日志。
不要把整个桌面总目录作为 VS Code 工作文件夹。搜索排除和 .gitignore 不能代替授权。

原桌面“孽岛”目录已归入 `../private-data/incoming/2026-09-03/`。
本轮按用户指定目录及其使用说明选定修订版 v1.0，原图/OCR/来源记录随快照保留，机制提炼仅作后台参考；另一部作品蓝图、脚本与旧备份排除。原有材料未覆盖，详细范围与已知限制见第三阶段 A 记录。
已有摘要和历史任务说明仅作参考；“某个schema存在”不等于已接入本运行链。
