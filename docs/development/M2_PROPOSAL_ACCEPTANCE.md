# M2：调查建议真实模型与首阶段输入核对

## 本轮任务约定

目标：核对五角色第一阶段可用材料，使用已经接入页面的 `PackageProposalModel` 原协议验证真实模型的调查选择和公开依据。向用户说明哪些能力已有证据，哪些仍未完成。

范围：新增独立的固定虚构样本验收脚本与回归测试；实际剧本输入对照保存在仓库外私有目录。保留现有提示、游戏协议、历史任务和模型配置。

不做：本轮不扩展后台 Audit，不发布商业候选，不将固定短句称为自然对白，不自动调查、计票、解锁回忆或修改主库。

完成条件：

- 五角色第一阶段目标、开场材料与条件回忆分开，有来源定位与摘要校验；编辑建议不混作原文规则。
- 固定八个虚构场景，调用前冻结输入、预期选择、允许/必需公开引用和模型元数据；评分标准不进入模型消息。
- 直接复用运行适配器的准备、单次调用、本地校验和公开渲染，验证两家已配置模型；每家最多八次、总预占不超过 ¥0.10，单次不超过 ¥0.02，未知结果停止、不重试。
- 默认只预览。执行必须同时通过既有付费开关、显式执行参数和输入哈希；同一冻结批次不能再次执行。
- 格式合格、固定样本判断符合预期和完整角色体验分别报告；真实失败原样记录，不追加提示迭代制造通过。

验证：

```sh
backend/.venv/bin/python backend/scripts/test_fusion_security.py -k proposal --disable-warnings
backend/.venv/bin/python backend/scripts/test_fusion_security.py --disable-warnings
backend/.venv/bin/python scripts/dev_preflight.py
git diff --check
```

人工开发复核：冻结真实结果中的选择、引用与原始分类；独立代理只读复核实现和来源报告。商业正文不进入公开测试或本轮付费样本。

风险与假设：这是小样本兼容性与行为检查，不能推算准确率或稳定延迟。原卡禁止出示不等于角色不能转述；现有分享契约不足以直接代表商业本的自然对白规则。未决规则继续保留，不阻断已获授权的工程工作。

## 首阶段输入核对

私有报告：`../private-data/import-jobs/m2-source-inputs/20260906T183759Z-source-audit/`（相对仓库根目录）。已定位五角色 31 项目标、25 条回忆；新核看 12 张原图，并复核来源定位和摘要。开场扫描页只做定位/字节核验，不能宣称全本逐字校对完成。

- 初始私读、后续回忆和调查奖励必须分别授权；第一阶段整理版含未批准的票制、信任、计分与时钟编辑段，不能整篇直接用作运行输入。
- 回忆依赖他人的实际发言或已获线索；自己念关键词、关键词表本身、尚未获得的调查卡都不构成触发。
- 部分回忆明确要求告诉大家，不能一律允许永久隐瞒；应在触发后记录角色转述为 CLAIM，原卡仍保持私密。
- 记录了一处人名 OCR 修正建议，未改旧文本或原件。来源哈希一致不等于这些内容已装入商业运行包。

下一片段优先实现一条有开场来源支持的“他人实际发言 → 本人获得私密回忆”路径，再验证“调查所得 → 回忆 → 角色转述 → 另一人回忆”的连续触发。不依赖未决票制或缺失卡的编辑补充。

## 本轮实测结果（2026-09-07）

调用直接使用现有 `package-proposal-model/1.0` 的准备、调用、校验和渲染；未改提示或运行协议。固定 temperature=0、输出上限600、thinking disabled；这是本批参数，不是更改游戏当前配置，也不代表其他温度或复杂长对话的表现。

| 模型 | 格式/权限校验接受 | 固定动作匹配 | 动作及引用均符合冻结标准 | 已知估算费用 |
| --- | --- | --- | --- | --- |
| `doubao-seed-character-260628` | 8/8 | 8/8 | 7/8 | ¥0.0063720 |
| `qwen3.7-flash-2026-07-15` | 7/8 | 7/8；另1条无法评价 | 5/8 | ¥0.00109276 |

合计16次、¥0.00746476，最大累计预占¥0.0735014。无重试，本批停止新增调用。样本单次观测约509–1723毫秒，不能当作稳定延迟或p95。

具体问题：

- 两家都在“私人目标改变”场景选择了预期地点，但引用的公开事实不能解释这个地点选择，违反事先冻结的空引用标准。引用存在且公开，不代表相关。
- 百炼在“无依据催促”场景保持了预期地点，却额外把催促纳入公开依据，未达到冻结标准。
- 百炼“具体新目击说法”场景被现有校验器以 `PACKAGE_ROLE_OUTPUT_INVALID` 拒绝，已按实际用量计费，未生成公开条目。未保存原始无效正文，不能断言具体格式错误、它选择了哪里或是否输出过敏感文本。
- 15条被接受的公开条目中未发现私密正文/引用泄露。玩家说法保留为 CLAIM 是程序渲染的结果，不能据此证明模型会辨认真伪。

结论：默认模型在本批具备基本调查选择能力，公开依据质量尚未整体通过；备用模型额外存在一次输出拒绝。保留默认与手动备用安排，不自动切换、不提高游戏 thinking、不把收集成功算作角色体验通过。自然对白、回忆触发、集体表决、商业本整段仍未验收，`runtime_ready=false`。

真实原始账本位于仓库外 `private-data/import-jobs/proposal-probe/`：

- Ark：预览 `20260906T184216Z-shzg3ro9`，执行 `20260906T184240Z-sx1vgnz7`；suite hash `9b8587458501e6046c7a381a7a1eb19c34f802422aaabf85bf34bd872f24d92e`。
- 百炼：预览 `20260906T184216Z-xs57h2yf`，执行 `20260906T184309Z-gaf6dhmx`；suite hash `5053e234689b6d9600479034317abd00e88df38efec2241749971d2cbcd2c2f1`。
- 更早两份零调用预览保留；价格与取消记账保护完善后重新冻结，未执行旧预览、未回填旧收据。receipt 的 `semantic_status=UNREVIEWED` 保持原样，独立开发复核另存，均不是用户出版批准。

## 工程验证与独立复核

- 新增67项双供应商离线检查；后端安全入口全套 **2018 passed**，363条存量警告，35.83秒，退出码0。
- `dev_preflight.py` 退出码0、自测16项通过，`git diff --check` 通过。
- 独立只读代码复核发现并已修复“缓存输入价格高于普通价会低估预占”：此独立验收入口现在拒绝该配置。新回归通过，复核未留 Blocker/Important。
- 来源独立复核：74个源文件摘要与121个JSON定位选段摘要一致。主代理另核对151个来源引用对象；不把这些摘要检查说成全本人工校对。
- 本轮未修改前端或运行模型协议，未重跑浏览器/前端构建；上一轮页面证据仍是模拟SDK，不能与本轮直连结果合称真实页面整链通过。没有主库迁移、服务启动或商业发布。

## 下一轮：继续 M2 的实际发言与私密回忆

交付：先把一条有原文依据的“他人公开发言 → 本人回忆解锁”接入持久事件和页面，并显示只属于当前角色的回忆提示。随后接入有限范围的角色自然转述，检验调查所得到多人回忆的连续触发。

实现方向：服务端判定触发、身份、阶段及是否已经授予；模型不决定知识权限。原卡正文保留私密，允许转述和必须说出的内容分别处理；自然对白使用独立显式契约，不修改旧问答、调查建议或历史哈希。引用相关性问题继续作为已知限制保留，不靠重复付费调提示拖住主线。

验收：真实发言才能触发，自己念词/未得卡/未来内容/错误角色不能触发；同一事件只授予一次，刷新与断线恢复一致，其他角色看不到私密正文。完成后提供可见片段与实际验证结果，再评估完整单阶段体验。

建议开发思考强度：**极高**，因为事件权限、原文规则和恢复需要一起核对；复杂交叉权限审查可用 Ultra。这是开发设置建议，游戏内 thinking 仍关闭。本轮及上述下一步无需用户先补充信息或重新授权；未决计分/票制规则留到影响实现的节点集中确认。
