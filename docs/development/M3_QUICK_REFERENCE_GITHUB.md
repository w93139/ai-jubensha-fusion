# M3 开场排版、随时翻阅与 GitHub 后端备份

用户 2026-09-08 要求：修复仍显示 Markdown 的规则标题；随时查看本人核心任务/回忆，关闭后继续原位置阅读；把后端保存到 GitHub，后续迭代同步版本；完成后重列验收项。

## 本轮约定

- 开场预览与试玩共用安全文字渲染。以截图中规则类标题及加粗列表复现，检查实际两个入口，不能只测工具函数。保留 OCR 原文，不改候选和剧情规则。
- 固定快捷入口打开任务/回忆/规则浮层，自身滚动，关闭或 Escape 回原位置、焦点回触发按钮。任务来自本人已获材料的明确目标段落，不由 AI 总结或自动判完成；回忆沿用授权与逐行显示过滤。手记与原存储继续兼容。
- 现有 origin 是公开仓库。采用当前账号的私有后端备份仓库，独立干净历史，保留当前原仓库和未提交工作。备份代码、测试、锁文件、迁移及必要运行模板/许可；不传密钥、数据库、日志、商业原件、OCR 或游戏存档。
- 已授权正常版本同步：每个完成验证的后端迭代，记录版本与文件摘要，普通提交和版本标签推送并核验远端 SHA。不安装后台监控、不使用强推、不把保存文件即视为验证版本。

## 验收与范围

预览回归/任务提取与权限反例/抽屉关闭复位/存储兼容测试，所有前端测试、类型/lint、预检16与后端默认安全检查、webpack、diff check；独立只读复核。真实浏览器在开场和试玩检查截图标题、列表强调、任务与回忆直达、阅读位置保持以及390px操作；保留现有本机存档，仅替换已验证前端。

GitHub同步工具用合成临时 Git 仓库验证白名单、敏感文件拒绝、删除镜像、同版本不变/冲突、远端分叉拒绝、提交/标签一致与可重复执行；实际私有仓库首次推送后读回可见性、提交和标签。此轮不需要新模型调用。

下一里程碑仍为 M3 用户验收，修复反馈后再进入 M4 五角色。推荐思考强度极高，因阅读权限和持久版本需联合核对。

## 实际交付

根因是开场预览仍以纯文本段落显示材料，正式试玩已使用 `PlayText`，不是仅需修改字号。已把预览简介、公私材料全部接入同一安全渲染器，补真实页面回归。真实浏览器检查截图两标题为20px、700字重，三项终局说明为粗体且无Markdown残留。另修中文 `1、内容` 无空格序号被OCR合行吞并的问题；不补写原文缺失的顿号。

预览提供固定「任务 / 回忆」，正式试玩提供「任务 / 回忆 / 手记」。随身资料含任务、回忆、规则页签，独立滚动；任务只从本人已获私有资料提取明确目标段，不从公共分享中混入别人任务。回忆使用已授权且台词显示完成的投影，不提前解锁。账号变化会清空预览和浮层，取消在途读取，阻止晚到响应和旧创建回调。

桌面实际在正文3200px处查看任务/滚动规则后关闭仍为3200px，焦点回任务按钮；390px窗口在2400px处打开任务、回忆、手记，关闭位置和焦点保持，发言框展开后快捷栏位于上方且无横向溢出。完整验证局的5条已解锁回忆可直接查看；新局0回忆空态正确。旧手记仍可读，存储方式未改。

私有仓库：[w93139/ai-jubensha-backend](https://github.com/w93139/ai-jubensha-backend)。最新版本 `m3-20260908.2`，提交 `610a3af3224c7e6b3b6567475bcbb8e755ced54a`，annotated tag对象 `3a474211d34b94b97b6c6c96ceffab923430f34e`；首次 `m3-20260908.1` 保留为父版本。406文件：362后端文件、36 JSON契约及8管理/支持文件。清单摘要 `6bc52dbf5c1a3df2a43fa6e7f2a6db96c6bf50bf9c52db05b4a14c34bb68d102`，GitHub读回与本地完全一致。原工程HEAD和公开origin未变；不导入旧历史、密钥、真实数据和商业材料。

首次上传成功后的远端读取曾失败，单独读回确认了版本已到达；保留该标签。第二版增加最多3次的读取重试，仅重试 `ls-remote`，不额外push。第二版普通提交及main/tag原子推送后，完整工具返回 `PUSHED_AND_VERIFIED`，再次读取私有属性和远端清单一致。后续同步约定已写入AGENTS，工具说明见 [备份流程](BACKEND_GITHUB_BACKUP.md)。

## 验证记录

- 前端307项；类型检查及7个修改源码lint无错误/警告；webpack构建通过。
- 后端2503项、实际导出副本2503项；第二版仅同步工具/测试/说明变化，362后端与36契约摘要保持。
- 同步工具27项，覆盖白名单、密钥/符号链接、删除、未知/脏目录、同版重试与异源拒绝、回滚、锁、原子推送与远端分叉、有限读取重试。独立复核另检查两种本地bare重试路径，均只有1次push。
- 预检及16自测、diff check通过；分工实施交叉复核无剩余Blocker/Important。
- 本机最终构建 `Cj-99Zs-kVhwo4gK7mgg-`，HTTP构建清单摘要 `f1d54b0c20d4ba17b946c39983d13821988af6feda08927f5a91cb58470b4bf8`；源文件、部署副本和服务构建一致。
- 本轮模型调用0、费用¥0；服务累计仍5次/保守¥0.06566376，后端未重启，现有游戏数据库摘要未变。只读回忆实测不等于新增关键词触发实测；后者保持回归覆盖，不宣称本轮完整内容语义已通过。

详细报告、GitHub回执、浏览器结果和新「验证流程.md」位于本机私有 `m3-user-validation` 目录；上一轮验收流程已归档。Ego任务69已关闭。入口仍为 http://127.0.0.1:18032/，等待用户按新清单验收。
