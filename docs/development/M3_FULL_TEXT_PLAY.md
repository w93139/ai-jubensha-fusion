# M3 完整文字整局

用户于2026-09-07要求完成M1剩余规则决定，并在没有特殊情况时持续推进到M3整局完成。沿用现有FastAPI/Next.js/PostgreSQL、单真人四AI及《孽岛疑云》，不另起PRD。

最新采用记录：用户明确回复“三项都按推荐方案”，[A/B/C玩法方案](M1_RULE_DECISIONS.md)全部采用。下方上一轮“待回复”保留为历史；当前不再依赖该确认。继续规则/来源绑定和整局实现，不逐项重新询问。

2026-09-08 最新：用户确认此次测试金额并继续开发。已恢复新引用格式四例的现存真实执行并完成独立复核：4 次请求、35914 tokens、估算 ¥0.0459048（上限 ¥0.50），预占归零；本次续接新增真实调用 0。格式 4/4 通过，J/S 所测内容通过，H 有源前提通过但反问有效性有限；Y 的记忆断言依据不足，药物不确定性与未知短醒时间两项未覆盖，整批语义未通过。新增禁网录制核验和普通/优化各 10 项检查、四个定向追问离线接线；相关后端 33 项、预检及 16 自测通过，两项独立只读复核完成。38 个本批及 581 个旧文件摘要未变；三批 M3 累计 13 次、108429 tokens、估算 ¥0.1381404。后续追问仅为离线案例，未获新付费批次授权；不重复收费、不安装 LiteLLM。M3 整局、M4 与正式发布仍未通过，runtime_ready=false。下一步 M3：定向验证未覆盖边界与电话回应有效性，再准备完整整局；建议开发思考强度极高。 [本次内容复核与离线开发](M3_CATALOG_REVIEW.md)。

2026-09-08 此前准备检查点：用户决定不接入 LiteLLM。已完成[新引用格式四例复验准备](M3_CATALOG_ACCEPTANCE.md)，套件 c6e17f38…dc8ae6 使用新对白1.5/电话1.2，四例免费演练、执行器普通及优化模式各13项、相关后端33项和两项独立只读复核通过。581个旧文件未变；新真实调用0、费用0，尚无新批次批准。当前可提交最多4次/40万tokens/估算¥0.50的新批次供用户批准；原两批保持关闭。下文为此前实现与实测记录，不代表新格式已通过真实模型或整局验收。

## 当前实现检查点：两次真实批次已停止，最新引用修复离线通过

A/B/C的采用回执保持原样。本次采用范围的规则、来源、有限答卷和评分对应已经落实；开发候选不等于正式发布批准。

- 整局链路：五席共同调查、轮换平票裁决、22+9点两轮调查、实际听闻回忆、五席独立封卷、31目标分项评分和个人结局。模型只提出受限选择/台词；扣点、权限、得分与结局均由程序执行。
- 私聊：AI可轮流决定邀请、暂不通话、回应或结束；旁人只见匿名进度。本线实际台词才触发听众回忆，固定邀请不触发。线路可以免费释放，迟到结果记账但不继续发言，刷新保留已提交回执。独立复核发现的暂停/恢复/转述记录问题均已关闭。
- 冻结边界：新局binding/1.2冻结整局输入65536字节和正式决策输出4096 tokens，普通对白额度不变。选项按实际知识与已听说法开放但不判真；图片和草稿随身份切换清理。旧模型协议、包、事件和失败记录保留。
- 当前素材：新候选摘要`51d31fa319932b08de073b72010846d6e069690698d0d795a76f188a463654dc`，5角色、94知识块、30卡、25回忆、29行动、31目标、31原图入口；145有限答题字段、74事实判据。323项来源文件核验通过；按原图将角色一处无依据的具体分钟恢复为未知，仅该知识条目/来源/版本改变，评分与结局完全不变，独立来源复核通过。关键图像文本、31目标说明及六类评分问题的定点语义复核关闭，18个私有反例通过。不是对所有OCR逐字无误的保证。
- 整局证据分版本：新候选纯引擎逐步重放186动作及18评分反例通过；旧候选dccf1ba…的隔离SQLite加三次AI电话模拟，189步骤、312事件、123次模拟SDK提交通过，阶段/封卷/结果保存恢复一致。模拟答案由测试脚本编写，真实生成0次、费用0。
- 页面（本轮未重跑）：旧来源候选完成原图、真人电话、五席调查、五席封卷、个人评分/结局与重新进入。dccf1ba…候选完成AI邀请/交谈/结束/免费释放和重新进入；390px宽度无溢出，测试后恢复1470px默认尺寸。页面与持久证据各自绑定原候选，不替换成新51d31fa…摘要；本轮新候选只追加纯规则/来源验证。临时API18022、前端13022已停止。

前一轮产品代码回归：后端2349通过（363条已有警告，45.41秒，`/tmp/m3-phone-security-full.txt`）；前端八套232通过（`/tmp/m3-phone-front-full.txt`）；TypeScript及修改的电话相关文件lint通过。页面构建26页通过，默认8010构建已恢复（26页，预载no-dotenv）；预检、16项自测和diff检查通过。独立电话复核后端232/前端90项通过，无新增P1/P2。

真实批次`8b828e90…8717454`已获用户“同意，继续开发”授权并执行：7次真实请求、0次调查、9控制步骤、16事件，估算费用¥0.0706632。第7次输出缺少必需依据，被INVALID拒收；001–006的格式接受不代表内容通过。独立内容复核：001/005/006通过、002保留措辞风险、003/004/007失败。保留原输入、返回、执行凭据及费用，不改写失败台词或追加请求。该批执行前的228步/384事件/156次模拟SDK仅是历史演练。

本轮修复验证：后端全套2366通过（363条已有警告，46.16秒，`/tmp/m3-strategy-full.txt`），新专项17通过；124个新候选策略输入容量检查通过，最大49715字节，必要策略未裁剪。公开回复/私聊/电话新版本已接线；旧协议、提示与历史状态按原版本恢复。两次独立SQLite副本检查均从真实16事件恢复，同键不重发，免费释放后17事件、费用相同，旧证据文件字节未变。独立代码复核发现的分派/策略复制保护问题已修复，无剩余P1/P2；预检、16自测及diff检查通过。无前端变化、真实追加请求或主库操作。

随后准备的[4例真实语义复测](M3_SPEECH_REGRESSION.md)，验证新供应商输出格式、时间线、转述归属、保密要求与不确定性。每例新上下文/新协议，原失败只作为回归依据，不是恢复旧请求；最多4次、40万tokens、预算¥0.5，遇错即停。该批已获“好的”批准，在第2例引用类别错误后停止，1通过/1失败/2未测；详见下方最新工作包，不能标M3完成或自动进入整局。

后台模型审核与正式发布门禁仍独立保留；无主库迁移、提交或发布，runtime_ready=false。下一步先做M3小范围真实语义复测，通过后再回到实际整局；建议开发极高，权限和评分终审可选Ultra。

## 2026-09-07 真实批次失败后的修复工作包

用户已批准一次真实批次；实际7次调用后因第7次空依据提问被拒收而停止，保守账本费用¥0.0706632、55430 tokens。独立复核发现前面已接受台词存在经历矛盾、转述冒充亲见，以及角色保密目标没有进入讲述决策输入。旧批次关闭，不重发或回填。

- 目标：新版本对白/电话在输出schema中区分有依据段落与明确未知；接入本人当时已解锁的私有目标/行为要求作为策略输入，继续禁止以它们作台词引用。
- 范围：对白/电话模型、整局策略投影、按模型版本恢复的服务接线及隔离回归；新协议显式启用，旧协议/提示/事件、原商业材料与已失败收据保持。
- 完成条件：缺依据在新输出schema即不合法；未知格式仍可用；本人目标和保密要求进入新策略区，其他席/未来材料不进入，策略不得成为basis或玩家视图；新旧事件可恢复、失败同键不重发、免费退出电话不改费用。提示改进不等于真实语义通过。
- 验证：后端安全全套、新版本专项、实际7次存档的隔离副本重放与免费退出、原始输出离线诊断和独立只读复核。无前端变化时不重复浏览器构建。
- 风险：模型仍可能捏造或违背角色要求；本地引用检查不能验证任意自然语言含义。需要新冻结批次实际验证，旧批次授权不能用来追加调用。未覆盖真实整局，M3保持未完成。

## 2026-09-07：四例复测停止后的引用目录修复

用户“好的”批准4例/¥0.50批次。实际执行2例后第二例把discussion编号放入knowledge，INVALID拒收并停止；17085 tokens、保守¥0.0215724，后两例未执行。保留套件98e9832b…及全部原始输出，不纠正旧输出或继续收费。

本次工作目标：新显式对白1.5/电话1.2让供应商schema直接限定当次合法的(collection,id)组合，避免只限定字段类型却任由模型错配目录。沿用原提示与角色策略，不改剧本或评分，不新增付费测试。范围为引用schema生成、窗口/预算计入、版本元数据及服务显式接线。验收：合法对通过、跨目录/未知编号/策略编号不通过；空目录保留明确未知；裁剪历史后按最终目录生成；旧prepared/元数据/事件保持可复验。运行新专项、后端全套、两条真实输出离线诊断与独立只读复核。只约束引用有效性，不保证引用能支持台词含义，真实语义及未执行两例仍未通过。

最新证据：后端2382通过（363已有警告、46.94秒，/tmp/m3-catalog-full.txt）；新增引用目录16项覆盖，旧相关40项通过。按原始返回逐字离线核对，新schema接受第一例、拒绝第二例错配，不修改结果；旧四份prepared/model逐项一致。124个同候选输入容量检查通过，最大50952字节≤65536，包含动态schema和必要策略。预检退出0、16自测和diff检查通过。未新增真实调用、前端变化、服务重启或主库操作。独立只读复核无剩余P1/P2，专项154项及最新引用16项通过。两批M3合计9次、72515 tokens、估算¥0.0922356；本次后两例及最新目录格式真实兼容性仍未测，新的付费批次未启动。

## 2026-09-08：新引用格式复验准备工作包

用户决定不接入 LiteLLM，继续现有 M3。目标是完成对白 1.5 / 电话 1.2 的独立四例验收准备，明确区分格式、实际内容和整局结果。原两批付费记录保持关闭；当前先离线准备。

- 范围：仓库外 `m3-full-play/20260907-strategy-candidate-2/catalog-acceptance-v1/` 的执行器、案例、预检和费用/停止条件验证，以及本开发记录。保留原四类内容判据，使用新请求身份；不新增游戏模式、依赖、模型、提示版本、候选编辑或后台审核迭代。
- 完成条件：四个新请求使用当次合法引用目录；候选、来源、模型/提示/参数、代码和依赖均可核对；旧输入、收据和批准不变。免费演练及失败反例验证单次执行、额度、首错停止、参数未替换、错误不会冒充语义通过。独立只读检查案例信息边界和执行器后，提供具体可审阅的真实批次范围。
- 验证命令（仓库根）：`backend/.venv/bin/python ../private-data/import-jobs/m3-full-play/20260907-strategy-candidate-2/catalog-acceptance-v1/check_guards.py`；同命令加 `-O` 检查优化模式；`run.py --dry-run --suite-hash <冻结摘要>` 检查四例模拟流程；`backend/.venv/bin/python backend/scripts/test_fusion_security.py -k 'speech_basis_catalog or full_play_speech_strategy'` 检查调用契约；预检、自测和差异检查。
- 风险与边界：离线 JSON Schema 接受不代表火山实际兼容，更不代表台词有据。真实执行仍需本新套件范围内的批准，不能复用已经结束的旧批次凭据；当前不修改 `.env`、主库或发布状态。免费演练返回值是模拟数据，不计语义通过。

## 目标与完成条件

完成有来源的整局：选角、分阶段阅读、公共讨论及原本支持的私聊、集体搜证、条件回忆、封卷、答卷/指认/信任、个人计分和结局，途中可保存继续。AI只读本人当时获准的信息作判断，服务器执行正式动作和评分。

完成须有规则选择记录、绑定的来源与私有整局材料、引擎/持久接口/页面接线、实际完整整局记录、费用/失败/恢复检查和独立复核。五种真人选角系统试玩仍属M4；一份片段或单独评分工具不得代替M3完成。正式商业发布独立保留审核门禁。

## 已确定 / 暂定 / 待解决

- 已确定：五席；两轮私读；共享22+9调查额度；原作已有钥匙/抉择条件；实际事件触发回忆；31项原目标与分值；提交后再揭晓；单真人四AI按角色信息判断；公开讨论和有依据的私聊分别隔离。
- 已采用：编辑修订v1.0与标注49-R的补卡、五席集体调查/主案票制、结构化答卷/部分分与个人结果。采用回执在仓库外 m1-adoption/20260907T072240Z，绑定玩法选择文档摘要；正式发布仍独立审核。
- 待完成：真实模型整局和逐条语义评估；规则、来源和页面已有绑定证据，不再重复询问A/B/C。

## 实现顺序与范围

已完成的 AI 主动电话工作范围：新增独立版本的有限电话决定与回执。空闲时 AI 可邀请任一其他席或暂不通话；接通后的模型只得到可讲述材料及本线实际发言，可继续说话或结束。玩家不在线时不显示参与者、私聊或引用，仅提供匿名继续/检查请求。使用既有预占和一次事件账本；旧正式投票和对白协议保留。验收包括两 AI 往返、第三席不可见、实际听众回忆、失败/费用耗尽后的释放线路、刷新和旧局重放，独立只读复核后再安排真实模型批次。

1. 复核M1剩余来源与玩法差异，准备简明决定表；无歧义的原作规则直接保留。
2. 先开发无具体商业答案的有限判定与封卷基础：严格输入、按角色私有表单、完整锁定前不评分或泄露答案；未知保留未判，不能从模型自报正确取得分数。
3. 在确认规则范围后接入冻结内容版本、整局引擎和一次性持久事件，保留旧包/旧局及失败历史。
4. 接入AI角色正式选择、私聊、玩家页面与结果说明，逐阶段整局验收；有实际证据后才标M3完成。

预期修改范围为新结算契约/纯判定器、包验证与运行引擎、持久会话/接口、角色决策适配和试玩页面，以及对应隔离测试。具体范围随来源与接口复核更新。禁止将商业正文、原始私本或付费收据放进仓库测试；不修改.env，不迁移主库，不自动发布或升级历史会话。

## 验证

- 仓库根：`backend/.venv/bin/python backend/scripts/test_fusion_security.py`，包含权限、原子封卷、未知/漏答、归票、计分、幂等、迟到和重放。
- 前端：沿用已登记八套组件测试，`node node_modules/typescript/bin/tsc --noEmit --incremental false`、修改文件ESLint与正式构建。
- 内容：原文明确规则、编辑方案和未决项分开；实际整局、模拟故障和模型实测分别记录，不能用人工偷偷填正确旗标代替自动结算。
- 集成：隔离数据库和回环页面；不访问主库。新真实模型批次须先冻结案例、预算及一次执行收据，旧M2已结束的20次请求不恢复重发。

建议开发极高，复杂规则与权限终审可用Ultra；这是开发建议，不改变游戏内thinking。本次采用范围的M1规则已落实；M3真实模型整局仍未完成。

## 2026-09-07：规则选项与封卷基础

### 实际完成

- 独立来源复核把必须决定的玩法压缩为三组，已给出无案件答案的[M1玩法选择单](M1_RULE_DECISIONS.md)，已向用户询问，当前尚无选择回执。来源明确的五席、两轮、共享调查、电话私聊和私密回忆等继续保留。
- 新增 `backend/src/fusion/finale_answers.py` 纯组件：按角色展示自己的题目，完整答卷一次封存，重复提交不重复生效，封存后不可改答；全部封存前不评分，不展示他人答案或答案键。
- 答案枚举由外部已核对计划提供，模型自报得分和额外字段均拒绝。未知答案或缺少判定键保留未判，不冒充零分、满分或完整总分。每席必须有评分项，避免遗漏某席细则时生成虚假完整零分。明确选择“不知道”是否零分仍取决于最终获选细则，不把当前 `None` 自动当成玩家主动弃答。
- 恢复时严格校验计划摘要并逐席重新验证；不同版本的答案键、分值、来源引用不能混用。该恢复函数只接收服务端保存的状态，事件真实性、局/用户绑定仍由未来接入的服务层负责。
- 仓库外素材机械核验覆盖181条来源记录、543项文件比对，542项直接匹配。唯一旧索引路径指向编辑现稿，已找到摘要精确匹配的原稿备份，单独记录原稿/现稿定位；不改写原文件或旧清单。整理69份角色阶段/答卷与卡片候选及61份可能需要鉴权出图的图像，编号均可解析；这不等于逐字OCR校对、完整语义或具体图像授权已完成。

### 验证与复核

- 最终专项：`backend/.venv/bin/python backend/scripts/test_fusion_security.py -k finale_answers`，42通过，2096未选。
- 最终后端全套：`backend/.venv/bin/python backend/scripts/test_fusion_security.py`，2138通过，363条存量警告，37.45秒，退出0。日志本机 `/tmp/m3-security-full-final.txt`。
- 开发预检退出0，自测16通过，`git diff --check`通过。
- 独立代码复核提出的漏评分席位与错题原因标记问题已修复，最终42项复验通过，无新增问题；规则说明独立复核后补充归票边界，并纠正原稿仅比较最高分的表述。
- 素材报告：仓库外 `private-data/import-jobs/m3-source-readiness/20260907T071115Z/`；独立来源报告 `private-data/import-jobs/m1-m3-rule-review/20260907T065710Z/`。文件身份一致不能代替内容批准。

### 未完成与下一步

这次没有新增玩家可操作页面；纯组件未接入包版本、实际服务/API、AI答卷或商业规则，没有启动服务、调用模型、迁移主库或发布。前端未修改，本次未重跑前端构建/浏览器，原M2证据保留。

目前实际依赖为用户对A/B/C三组玩法的选择。收到后保存具体采用记录，继续M1逐项来源与答案/证据核对，并推进M3版本绑定、集体调查、电话私聊及视觉材料授权、正式封卷、计分和结局的整局接线。交付物是从选角玩到结局并可保存继续的一局；以有来源整局记录、恢复/迟到/费用检查和独立复核验收，不能用本组件测试代替。建议开发极高，复杂权限与评分终审可用Ultra。

M1尚未完成；M2有限片段已完成；M3尚未完成；M4及正式上线未验收，`runtime_ready=false`。
