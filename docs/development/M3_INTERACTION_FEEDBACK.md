# M3 调查建议、互动轮次与阅读动作反馈

用户要求：说明调查建议是否已实现并改善不可用提示；隐藏Token和人民币预算，玩家只看互动轮次及字数；与剧本无关的问题不回答；已阅读的私密经历/回忆随时能找到；修复材料收藏和提问操作无反馈。

本轮目标是继续完成M3反馈修复并交还用户验收。保留既有游戏、冻结候选/模型历史、角色/回忆权限及收费账本；不进入M4、TTS或正式发布。

## 观察与实施约定

- 调查建议已有模型、合法选项及保存链。要根据服务端具体原因和当前步骤提示下一步，不能笼统归咎于模型/额度，也不能绕过调查阶段条件。
- Token/CNY上限留在系统；若显示轮次，必须使用服务端真实计数与限制，不按余额猜测。输入按Unicode字符校验，前后端一致。
- 当前模型仅用获准资料作答，并有有限元信息拒答；新增明确跑题预检，保护普通剧情提问及旧事件重放，不将有限规则包装成任意语义识别。
- 右下回忆只统计已授予卡；已知角色经历、明确触发清单提供独立可读入口，不把读过关键词视为已解锁回忆。
- 正常可视点击收藏已复现成功；过期/加载状态的静默丢弃路径要阻断并给提示。材料提问应显示本地参考上下文、扩大可点击范围；不自动发送私本原文，不在未确认前请求AI。

## 完成条件与验证

前端回归覆盖来源隔离、回忆时序、按钮状态/反馈/本地提问上下文、无技术预算和真实轮次显示；后端覆盖真实轮次、字数及明确跑题拒绝且不收费、不写无效模型事件；保持旧种子重放。完整所需前端/后端/预检16/类型/lint/webpack/diff检查及独立复核。Ego真实浏览器复核阅读阶段和调查阶段、收藏/提问、已知提示与已解锁卡，并验证正常相关问题和跑题拒绝的差异；必要费用在既有授权内。

必要后端修复必须实际应用到本机服务，保留旧数据库并复核恢复；完成后同步到私有GitHub新版本，不重写旧tag。更新具体验收流程后停在M3；后续思考强度建议极高，因为输入边界、历史回放与计费须一起核对。

## 本轮实际交付（2026-09-08）

- 调查建议不是未开发；当前候选的阅读步骤没有调查动作，禁用符合规则。现按阅读、终局、在途请求、无共同目标、轮次上限或当前角色分别提示；阅读态可直接前往正文末。无需先保存公开发言。
- `include_interactions=True` 只读扩展投影统计实际 AI_REQUEST（完整局上限400，旧引擎30）。HTTP及本机验收服务显式开启；原默认视图、binding、模型元数据、旧事件与回放不变。玩家不渲染Token/CNY；内部预算仍生效，400不承诺剩余可用次数。
- `package-request-scope/1.0` 检查新ASK或实际获准的公开/私聊回应目标，以及电话当前真人目标；明确外部任务/元信息请求在prepare、预算预占和AI事件前返回422。旧已存幂等请求先恢复，不按新规则重判。有限模式不等于任意跑题语义分类，已有获准材料约束继续生效。
- 回忆页分已授予正文和触发提示，增加角色资料页查看当前本人全部授权材料；不把读关键词或开场经历当作新的回忆授予，保留逐行台词因果等待。
- 材料收藏/提问按钮44px高，失效时禁用并提示；提问打开底部发言面板并显示本地原文参考，不自动发私本。旧请求明确未派发时释放重试状态，未知结果仍用原幂等标识。错误提示滚动时可见，私聊/公开/问答一致按Unicode字符上限检查。

## 证据与修复复核

前端全套321通过；最后仅调整错误提示可见性后相关105通过。后端与干净GitHub代码导出各2545通过（既有363弃用警告），预检退出0、自测16，完整类型、修改文件lint、webpack、diff检查通过。独立复核提出的电话拒绝后旧attempt、失效发言收藏、私聊trim边界已修，相关141独立用例通过，无剩余Blocker/Important。

Ego任务70真实操作：调查步骤生成并保存1条建议，轮次5→6；两次明确外部任务请求拒绝且仍6，1001字不可发送；最终390px拒绝提示y80、宽358高106，草稿可编辑。收藏显示本机已保存/已收藏提示；资料提问显示参考并聚焦输入，不发送。资料浮层关闭前后正文scrollY均6435.5；390px四页签均在视口、浮层x12到378，工具栏底393.52位于输入面板顶416.52之上。阅读步骤提示及正文末链接正确，旧规则标题20px/700；完整存档显示5张本人回忆，新局显示0张及触发清单。

本机实际服务已更新，入口仍http://127.0.0.1:18032。最终前端构建`Lmc0jVijTRUdOZtTm7rNA`，manifest SHA `f4b59b1a312c7082240ff705fc37dcdcc7abd672f355cc85e5bf306d5127f06d`。数据库一致备份在验收目录`interaction-feedback-before`；重启前后全部表行一致。最终439条旧事件全部逐行保留，仅本轮建议新增2条事件；冻结终局投影除新计数外逐项相同。新增真实模型调用1，保守费用¥0.01075440；不运行新的完整整局、不声称新触发回忆或语义全过。

后端已同步私有https://github.com/w93139/ai-jubensha-backend，`m3-20260908.3`、提交`441d82e82c072e5a0ee8eeda135d67a18619a5e6`、标签对象`79614be82563ba8c80a4505ab9825ca1c1d74047`；408文件，清单SHA `dbd7dea24b3b130b3b9d5d04c92b77cbd16b0bcabdf5e5afc61a84b5e9953706`，PUSHED_AND_VERIFIED且远端Private复核。原公开origin与旧标签不动，未上传私密配置/商业资料/存档。

私人验收目录的`interaction-feedback-validation-report.json`记录可复查摘要，`验证流程.md`已重写本轮优先验收项，旧流程另存。停在M3验收，后续先处理用户反馈及既有内容质量问题，提供更新页面与复验流程；尚未进入M4或正式发布。建议下一轮思考强度极高；无待追加授权事项。
