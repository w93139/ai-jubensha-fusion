# M3 第一幕「你的表现」单卡列表

2026-09-10 最新用户纠正：每条内容较短，直接列在一格，不再使用翻阅交互。已改成同一张「你的表现」卡片中按原顺序直接列出全部3–7条；移除编号切换、上一条/下一条、选中状态、内层大卡片及内部滚动。保持原文与所有图片，逐项用“需讲述／可选择讲述”保留转述义务区别，共用一次不能出示原本的提示。

本轮仅1个生产组件及2个现有测试变化，原分组/主页面/收藏数据与后端运行时代码不变。前端仓库必需套件及相关测试352项、后端2807项、TypeScript、组件及相关测试lint、预检16项、隔离构建和独立只读审查通过。实际浏览器桌面/390px DOM均为5项同时存在、0翻阅按钮、0标签页、0内部滚动、1条通用提醒；五项中1条需讲述、4条可选择讲述。页面样式CSS可正常读取。一次视口测量的顶栏/底栏未处于固定布局，因此不将该几何样本作为固定布局验收依据；没有截图或实体手机验收声明。

本机 http://127.0.0.1:13032/ 已更新构建`6dOKVL_if1w0tdRvR9e6o`，后端未重启；911事件、25存档及全7表保持，本轮调用/费用0。浏览器空间7已结束。证据位于`m3-user-validation/performance-list-20260910/`。PRD0.21与文档保存至后端私有快照`m3-20260910.4`，远端核验在本机final-report和项目最新交付记录中；该快照不包含前端。

当前停在M3等待用户测试。下一轮按反馈做页面修复复验，建议高思考强度；无需用户另做决定。本节覆盖下方旧翻阅设计。

---

## 历史：此前翻阅版

2026-09-10。用户指出第一幕读后的总结把标题、总说明与编号内容拆成多个大卡片，并逐段重复收藏按钮，页面过长。目标是统一标题和总说明，以编号及前后按钮翻阅每条完整要求。

## 范围与验收

仅调整当前角色已授权的未公开阅读资料展示，不改原材料、模型输入、规则、阶段、存档或收藏存储。原先已有的收藏仍能通过收藏工具访问。

可验收行为：一个「你的表现」标题、一段原文总说明；1至实际条数的编号切换、上一条/下一条；每次仅一条完整正文及对应转述提示；不在此组逐段放收藏按钮。手机竖屏编号可横向滚动，选择越界编号时只滚动编号条，正文与页面不被强制跳转。长条目内部可滚动阅读。普通资料、公开分享及授权图片保持可用。

## 实现

- `playPerformance.ts`只接收当前已授权私密知识。识别独立「你的表现」标题、至多一段总说明和从1连续递增的材料编号；只组KEEP_PRIVATE且不可分享内容。支持全角句点编号，保留每条多行原文，不按原书页划界。缺标题、跳号、可分享项等不支持结构回退原显示。
- `PlayPerformanceSummary.tsx`提供数字标签页及前后切换。保留逐条MUST_RETELL、MAY_RETELL区别和当前材料图片；顶部与总说明的授权图片也保留。无收藏或游戏写入回调。
- `PackagePlayPanel.tsx`只在未公开knowledge分支使用分组；以账号/局/角色/阶段/组为组件身份，阶段或角色更换重置选择。原收藏available数据和收藏键不变。
- 使用真实材料只读核对：两绑定版本×五角色共10组，T5/H6/Y4/J7/S3；原对象、正文和顺序全部一致。没有复制商业正文到代码或测试。

## 验证记录

证据目录为仓库外`m3-user-validation/performance-summary-20260910/`。执行相关前端组件/整套测试、TypeScript、修改文件lint、仓库后端安全套件、预检及16自测、独立只读审查、隔离webpack构建，之后在原13032入口完成1470px桌面与390px竖屏实际交互。

- 前端全套507项通过；数字局部滚动小修后的最终相关210项通过。TypeScript、生产修改文件与新测试lint、预检16项、后端安全2807项及隔离webpack构建通过。
- 现存大测试文件有旧`module`变量被Next专用lint规则报错；仅测试文件检查关闭该规则后通过，生产代码维持默认规则。失败日志保留。一次新增测试过度断言具体scrollLeft，已修为实际编号可见性；最终相关测试通过。
- 独立只读审查无未解决Blocker/Important/Suggestion；10组真实来源核对保持。所有数字和前后按钮共用编号条局部滚动，避免影响页面位置。
- ego-browser实测1470px：一个总结区、5个编号、1条详情、0个此组收藏按钮；数字1–5、前后端点、Home/右键/焦点通过。总结区内部高度360px，原7卡区域连同间距高度1522.5px（不同外框口径，不用于计算精确缩减比例）。
- 390px竖屏：页面宽度390px，无横向溢出；第一条总结区高486px，底部665px，发言面板上边783px，无遮挡。数字点选、上一条、刷新恢复可读内容通过，当前游戏阶段保持。截图接口再次超时，已恢复视口，采用真实DOM几何与快照留证，不声称截图或实体手机验收通过。
- 本机前端已更新为`yyhiJLO4PIOHfGyFhm9wQ`，后端未重启；原7表、911条事件、25存档完全一致。本轮新增游戏事件0、模型调用0、费用¥0。浏览器任务空间6已结束。

入口：http://127.0.0.1:13032/ 。刷新后查看原局的阅读资料即可体验，无需新开局。PRD0.20与本记录作为后端文档私有快照`m3-20260910.3`保存；快照不包含前端，最终远端核验以项目最新记录及本机`final-report.json`为准。

当前仍为M3用户反馈修复。交付后等待用户测试；下一轮按具体反馈修复和复验，建议高思考强度。实体手机键盘与真实语音仍是既有待验项，不启动M4或横屏功能。
