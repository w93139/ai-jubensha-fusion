# M3 四例角色对白真实复测

2026-09-08：本文记录的旧批次继续关闭。新对白1.5/电话1.2的独立四例已完成冻结及免费预检，见[新引用格式验收](M3_CATALOG_ACCEPTANCE.md)；新批次尚无批准或真实调用，不能复用本文的旧授权。

本批已获用户“好的”批准并执行，在第2例非OK后停止，批次关闭。上一整局批次已在第7次非OK后关闭，消耗估算¥0.0706632；本批是新版本、新输入、新执行凭据，不恢复旧请求，也不自动启动下一局。

## 实际结果

执行套件98e9832b…保持；仅2次真实请求、17085 tokens、保守费用¥0.0215724、无剩余预占。结果为 **1通过、1失败、2未测**：

- 第一例：经历叙述有据，必讲内容完整，格式和独立内容复核通过。
- 第二例：本次生成保住角色秘密、未将转述变成亲眼见，但将discussion里的两个发言编号标作knowledge，因此整体INVALID。不得人工改引用后回填为通过。
- 电话及不确定性两例没有执行，仍未覆盖；不能用上次离线或本次其他台词代替。

原始dispatch/response、模型结果、用量、批准与单次执行凭据保留在本批execution目录。没有第3次请求，不重试、不自动开启下一批。此失败不是供应商不可用；本批验证了1.4基本新输出形状可返回，但没有证明引用内容全正确。

后续离线修复采用对白1.5/电话1.2：在供应商schema里枚举当次合法的引用类别与编号组合，原提示不改，旧失败不修补。新本地schema对第一例原样接受，对第二例原样拒绝；旧四份prepared/模型元数据完全相等。后端2382项与124个输入容量检查通过，不能替代真实新格式验收。详见[M3记录](M3_FULL_TEXT_PLAY.md)。

## 原批准范围（历史）

固定使用火山方舟北京 `doubao-seed-character-260628`，仍为非思考模式、temperature=0、单次请求、无自动重试。只发送当次角色获准的文本、本人内部策略及当时实际听到的说法；原图和其他角色私本不发送。4个独立片段来自首批暴露的问题：

| 片段 | 检查重点 | 通过要求 |
| --- | --- | --- |
| 公开回应：经历 | 不能把局部经历扩大成与后续时间线矛盾的结论 | 至少一段相关有据回应，另核对本次可履行的必讲要求 |
| 公开回应：保密与归属 | 本人保密要求进入决策；别人说法不能变成亲眼所见 | 有效回应同时遵守本人保密要求，不能朗读内部目标 |
| 电话追问 | 缺依据、问题中暗含未提供的事实 | 非未知段落均有相关依据；确实不知道可以承认 |
| 公开回应：不确定性 | 原文“似乎”与未知时间不能变成确定事实 | 至少一段相关有据回应，保留原有不确定性 |

三条公开普通问题不能以全空UNKNOWN获得语义通过；程序格式OK也不是内容通过。对每条实际生成核对冻结输入和来源，保留失败及原始输出。历史错误台词在复测上下文中仍只是原发言者的CLAIM，不为它们改写角色事实。未实际谈到的药物/醒来时间等具体子项记为未覆盖，不能以没有出现失真宣称已验证。该小样本不验证整局、所有角色表现或后台模型审核。

## 费用与停止条件

- 最多4次SDK请求、400000 tokens、保守估算预算 **¥0.50**；普通输出最多1000 tokens、输入最多65536字节。
- 沿用2026-09-05保存的北京角色模型价格证据；统一采用较高档输入1.20、缓存0.16、输出6.00元/百万tokens，价格证据摘要进入新套件。预算不是效果承诺。
- 每次调用前持久预占；未收到确定用量按预占保留。第一条非OK、异常、超时或超额即停止，旧键不重试，不换模型或重复调提示。
- 所有格式接受结果之后仍须独立内容审核；通过后再准备M3完整整局，不自动追加付费批次。
- 默认只做禁止网络的准备；执行前需匹配套件摘要的明确批准及单次执行凭据。只在受控进程中开付费开关，不修改.env、不操作主库或发布。

## 执行前冻结与离线证据（历史）

候选`51d31fa319932b08de073b72010846d6e069690698d0d795a76f188a463654dc`，来源包`9509f897d022fc5c34cc1679f12f3ed872516716e670404d1ca5dedbbab7993d`。

最终复测套件`98e9832bd402b72744da5ebd1c0c31b01b14739d287f48b8e297526932ebe176`冻结4个输入与prepared请求、模型/提示/schema、代码、依赖、执行器、费用证据和保护检查。目录位于仓库外 `private-data/import-jobs/m3-full-play/20260907-strategy-candidate-2/semantic-acceptance-v1`。

最终摘要的免费演练完成4例，真实调用0；正常及Python优化模式的6项授权/额度/单次领取反例各通过。执行器独立审查无P1/P2，最终4例材料的独立预检通过；先前摘要0eea1de6…的演练不替换为最终摘要。免费演练返回UNKNOWN仅检查结构、单次执行和费用流程，不计语义通过。

产品代码后端2366项、新策略专项17项通过。新候选323来源检查、186步纯规则演练、18评分反例及124个策略输入容量检查通过（最大49715字节，未删必要策略）。旧真实16事件存档副本重进、同键检查和免费释放到17事件通过，未追加模型费用或改旧记录。

下一步属于M3：先验证新引用目录格式并补齐未测项，再回到完整整局；本批已关闭，不以旧授权重发；交付为有来源的实际台词/失败与费用记录。建议开发思考强度 **极高**，复杂权限复核可用Ultra；这是开发建议，不是更改游戏内模型thinking。M3真实整局、M4及正式上线均未完成，runtime_ready=false。
