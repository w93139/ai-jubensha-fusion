# M3 已执行四例的内容复核与后续开发

## 目标

完成已获批准并已实际执行的新引用格式四例验收，分别说明格式、台词内容和覆盖范围，依据失败证据推进必要的离线开发。

## 范围

- 恢复 `catalog-acceptance-v1` 的现存批准、执行凭据、四次响应和费用；本轮用户再次确认此次测试金额，现存批次不重复执行。
- 核对套件 `c6e17f38fad5228c3f6bdf7911a41a944e02d4eea3e82af51fcaa8cc85dc8ae6`、冻结输入、来源和实际输出，独立复核 J/S/H/Y 四例。
- 如发现问题，只根据可复现证据完善离线验收或必要实现，并记录具体范围；真实失败保持，不以改写输出或放宽判据取得通过。
- 更新验收及交接记录。商业正文与实际模型响应保留在仓库外私有目录。

## 不包含

不安装 LiteLLM，不追加新付费批次，不自动运行真实整局，不操作主库或发布，不重写旧批次证据。

## 完成条件

1. 四份响应与冻结请求、来源、费用账本可核对，未知和未覆盖项明确。
2. 台词语义经独立只读复核；格式通过不代替内容通过。
3. 必要修改通过相关离线检查，并保留历史证据摘要。
4. 交付可阅读的结论、已完成与未通过项，以及下一轮 M3 的具体计划和建议思考强度。

## 验证

- 使用禁网的只读验证重算套件和历史摘要，重放录制响应；不得调用 `--execute` 或重跑准备覆盖旧文件。
- `backend/.venv/bin/python scripts/dev_preflight.py`
- `backend/.venv/bin/python -m unittest discover -s scripts -p 'test_dev_preflight.py'`
- 如有产品代码变更：`backend/.venv/bin/python backend/scripts/test_fusion_security.py`
- `git diff --check`

## 风险与假设

现存报告为 `SEMANTIC_REVIEW_PENDING`：4 次真实请求，35914 tokens，估算 ¥0.0459048，预占归零。此报告尚不证明四例内容或 M3 整局通过。费用按冻结单价估算，并非供应商实际账单。本次不因预算尚有余额自动追加请求。

## 本轮完成结果

- 已完成原四例内容复核，追加私有报告 `catalog-review-v1/semantic-review.json` 与 Markdown。原执行报告保持当时状态；J/S 所测范围通过，H 有源前提通过并保留重复反问备注，Y 内容不接受且两个不确定性目标未覆盖。
- 新增禁网录制核验 `verify_recording.py`，核对套件、来源、批准/执行凭据、实际请求、响应 schema、收据重放、费用和预占。10 项正常/反例检查普通及 `-O` 模式均通过，涵盖录制改动、请求变化、返回截断、用量错配、少案例、费用漏记、预占未释放和片段冒充整局。
- 新增 `prepare_followup.py` 与 `check_followup.py`：四个独立身份的定向追问，加入录制台词时仍记为 CLAIM；本人材料/策略和原模型/提示保持，评价标准不进入请求。四次模拟 SDK 接线通过，最大输入 36254 字节；没有新的真实模型回答。
- 新鲜相关后端命令 `backend/.venv/bin/python backend/scripts/test_fusion_security.py -k 'speech_basis_catalog or full_play_speech_strategy'`：33 通过、2349 未选、13 条已有警告；开发预检退出 0，预检自测 16 通过。
- 独立语义复核由 `/root/catalog_semantic_review` 完成，保留 Y 的重要未解决内容问题；工程只读复核 `/root/catalog_evidence_review` 重跑录制核验、普通/优化 10 项及四个新片段，无 Blocker/Important，仅认可证据工具与离线准备。
- 38 个当前批次与 581 个历史文件摘要保持。新增真实调用 0；本批已执行费用估算 ¥0.0459048，三批 M3 合计 13 次、108429 tokens、估算 ¥0.1381404。

本轮新增的是验收工具和定向案例，没有修改产品运行代码或提示版本；前端未变，未重跑页面/构建或后端全部 2382 项。模拟和录制重放不证明台词内容通过。M3 整局、M4、后台审核与正式发布仍未通过。

## 下一轮计划

所属里程碑：M3 完整文字整局。先将四个定向边界整理成可实际执行的验收方案，冻结代码/来源/模型、费用和首错停止条件，验证没有漏掉目标或把 UNKNOWN 当通过。可见交付物是逐项来源、实际回应、覆盖结论和费用记录；内容边界通过后再进入完整整局记录与保存恢复验收。

建议开发思考强度：极高。重点是区分角色合理表达与无源记忆、有效回应与有据反问，并保持来源、费用和历史版本一致；此建议不表示已切换会话设置，也不改变游戏模型 thinking。

目前没有需要重新决定的玩法或已执行金额。后续真实付费范围尚未形成新批准；四个离线片段不能直接收费执行。常规离线开发继续沿用既有授权。
