# M3 引用与私聊转述修复

2026-09-10。用户授权自行修复已复现的回答质量问题，完成后停止开发等待用户测试；手机以竖屏为验收范围，不增加横屏切换或锁定功能，取消横屏专项必验要求。

## 任务约定

- 修复 S 已知部分事实未引到支持段落，以及 H 核对类私聊重复完整原卡的问题。原案情、权限、原卡复制保护、旧存档及失败收据保持。
- 增加可选 `topic-answer-check/1.1`，由有源议题目录声明已知事实表达与支持段落的对应。命中时同段须选择所需引用；漏引即拒绝并保留账本，可由玩家选择有源短答。该规则不等于通用语义判真。
- 只在新问题冻结新契约和新版披露说明。旧 1.0 提示、请求、上下文哈希及结果不重判，不因改目录改写旧绑定，不自动重发。
- H 根据初问／经过／核对分别组织短答，不反复要求按原卡顺序讲完整时段；不降低 24 字原卡复制拦截标准，不提供可以直接照抄的范句。
- 同批回归发现的 H 附带观察漏引、Y 将他人“似乎受惊”升级为确定，以及 J 省略动作主语的歧义，一并纳入定向修复。Y 保留本人真实情绪与他人心理推测的区别；J 明确各句人物及听闻归属，不把计划写成已经发生。
- 范围：表达校验、1.1 专属上下文说明、虚构回归测试、仓库外新内容目录注册与本机运行接线、现有 PRD／验收交付记录。不增加模型供应商、路由模型、TTS、ASR 配置、数据库迁移或 M4。

## 完成与验证

1. 漏引／错段／引用在另一段／越权和无效目录的反例被拒；合法引用与无触发回答通过。新公开及私聊失败仍可取短答，费用和幂等保持。
2. 旧 READY／PENDING／OK／FAILED 记录和实际全部既有存档可恢复；冻结请求不因目录改变而重算为新请求。
3. 在隔离新局做有限真实样本，逐条核对归属、所选来源覆盖与无原卡复制。S 和 H 目标样本实际正常保存，不能用固定答复替代并称模型修复成功。
4. 必需后端安全套件、前端相关交互与类型检查、预检及其自测、独立只读复核通过；本机更新前备份，核对原表行与旧事件，页面真实检查后提供用户入口。
5. 验证后的后端按既有授权存入私有 GitHub；正文、原始材料、音频、数据库和密钥不进入快照。保留本次成本、失败与未测项。

真实生成最多两批，每批最多 7 次，整轮估算预算上限 ¥1；派发前累计已记账费用与本次预占检查，无自动重试。必要的新批次须来自具体失败修复，不能为获得好结果无限抽样。

交付后停在 M3 用户测试，不自动进入 M4。真实 ASR 和竖屏实体手机键盘仍按各自证据验收；不把本轮语义修复当作其通过。

## 最终实现

`topic_answer_checks.py` 新增可选 1.1 的 `conditional_basis`。每条声明包含有限表达词和必需的资料／片段引用；目录加载时验证资料属于该角色当前议题、片段确实存在、列表和词长受限。输出按段核对，不能用另一段的引用补足，也不会替模型补引用或改写其原始输出。

1.0 的提示文本、模型契约和请求格式保持。只为已经冻结 1.1 的问题追加新说明；目录改版不影响旧问题、旧待定请求或结果。原卡复制保护不变。

私有 v8 目录保留原材料、解锁、可交流对象、次数和固定短答，仅调整四个相关回答对象的表达指引与可选检查：S 引用覆盖、H 简短转述和本人观察引用、Y 他人受惊的不确定性、J 搜捕／收好处两件事的明确主语。没有新增剧情事实或自动公开私密内容。

## 验证记录

- 新增 34 项虚构数据回归，覆盖非法目录、同段／跨段引用、新问题冻结、公开／私聊拒绝与费用、正确结果保存，以及旧 READY／PENDING／OK／FAILED 恢复。
- 必需后端套件 2807 项通过；前端相关交互 201 项、类型检查、预检及 16 项自测通过。产品前端未修改，沿用已经核验的构建。
- 六条失败→短答→追问→调查→五席封卷→结算→恢复的禁网整链在新代码下全部通过。
- 两个版本目录下全部 24 份既有游戏投影逐项一致。旧漏引及复制样本保留；用新契约检查时，旧 S 漏引、H 附带观察漏引、Y 遗漏限定分别被拒，原 H 复制仍被原保护拒绝。
- 第一批 7 次实际调用中，原 H 私聊复制和 S 初问漏引目标样本通过，但独立复核发现三处附带表达问题；该批记录为 4 通过、1 引用不足、2 措辞谨慎，不能改为全通过。第二批针对这些具体问题冻结 v8 后再测。

私有证据位于 `m3-user-validation/semantic-fix-20260910/`：源码与数据备份、v7/v8 目录、逐批冻结输入与输出、费用、独立来源/语义审查、旧失败反例及部署核对分别保存。商业正文不进入后端快照。

## 本次交付结果

第二批 7 条真实回答全部通过独立来源复核，均为正常模型结果，没有使用固定短答替代。S 的实际回答包含饮酒后的表现，并在同一段选到两处必需来源，覆盖了此前漏引的真实分支；H 公开与私聊均合法转述，Y 保留他人心理推测的限定，J 的动作主语和听闻归属清楚。J 的正常答复实际触发 Y 的授权回忆，下一次请求包含该回忆。该局完成 29 次调查并到达终局；五席封卷、结算和恢复由上述六条离线整链另行验证。本轮没有再次付费跑终局。

两批共 14 次真实调用，估算费用 ¥0.05994168（第一批 ¥0.03253512，第二批 ¥0.02740656）。第一批的问题和此前历史失败仍按原判定保留，不能用最终 7/7 推断任意问题的准确率。

本机服务已更新，入口为 http://127.0.0.1:13032/ 。最终目录指纹为 `6828b0f32f61704316158991c488c7b77471291a9d83ca83e3ab1d3a43da5752`。前端未改动，沿用 `Ete94GO-cc256CfNqep-T`；部署前备份并核对既有数据库行保持一致。ego-browser 实际打开首页、恢复旧局及新游戏选角页，390px 竖屏页面无水平溢出。没有创建新游戏或产生额外模型费用，也没有把模拟尺寸检查标为实体手机通过。

后端代码和无商业正文的交付文档按既有授权保存为私有快照 `m3-20260910.1`；最终提交与远端核验结果另记在本机 `final-report.json` 和项目最新交付记录，导出副本须通过同一后端安全套件后才推送。

用户测试：从首页“开始新游戏”选择唐小姐，按原流程调查、公开交流和单独对话，重点感受回答是否贴合问题、是否重复照抄以及追问能否继续。已有问题保留原请求和原回答，因此测试修复应使用尚未发起的新问题；已经问完相关题目的旧局不自动重新生成。失败时按界面选择有源短答，可继续追问、调查和终局。发现异常时提供角色、阶段、问题和页面表现即可定位。

当前停在 **M3 已知回答问题修复后等待用户测试**。不增加手机横屏切换或锁定，不执行横屏专项。真实 ASR、实体手机竖屏软键盘、M4 五种真人选角及正式发布仍各自待验，不自动继续开发。
