# 阶段 4C：真实链路与原本规则接入准备

2026-09-06，编码前计划。用户要求后续在已有授权范围内持续开发，确实需要其操作、授权或补充信息时才停下来说明，最后统一介绍。持续目标仍是一名真人与其余 AI 按《孽岛疑云》原规则完成整局，含角色互动、完整结算以及后续语音；不能把 4B 的资料选择器或虚构夹具当成最终产品。

## 本阶段结果

1. 用新的小虚构包验证 4B 模型契约：真实云模型 → 受限引用 → 确定性分享 → 持久记录 → 玩家可查看 → 终末指定揭晓。预先生成可核验输入与费用上界，单次请求、无自动重试；失败记录保留，禁止以另一渠道自动替代。
2. 在已经识别的本机测试库里，用独立随机 schema 检查新包表结构、双连接预算/版本冲突、提交后崩溃恢复及过期结果。没有商业数据，不迁移或清理主 schema，不停止原有数据库服务。
3. 核查原始主持资料、规则页与已有机制提炼，保存来源锚、阅读范围、明确规则和未解决问题。由原本证据决定下一版阶段/动作/结算契约，不能从当前少量字段反推故事规则。

## 已确认与边界

保留 FastAPI / PostgreSQL / Next.js。现有默认角色渠道与已配置密钥沿用；不更改 .env，不在日志回显密钥。用户此前已同意所选官方渠道与真实付费开关，本次真实验证仅在既有开发/测试范围内用虚构资料、明确上界进行；不启动额外商业批处理或部署。

4B 的模拟材料问答已经验过，但新的模型 wire 尚未真实测试。本阶段先补该证据，再继续自然角色对白和原本特有机制；独立数据源、审核门禁及旧开场/演练/文字试玩版本均保持绑定，不就地改变历史权限。

采用 juben-design 的资料审查/玩法提炼部分。其适配器文件包含旧副本的实现描述，当前实现仍以本仓库代码为准；不把旧 blueprint 宣称为已接线。本次做原本软件适配，不创作新故事、不改写商业原文，不以模型审核替代用户的最终内容确认。

## 启动和验收

真实模型和 PostgreSQL 各使用独立、显式安全入口，默认仅预检/离线；完成代码后将精确命令、费用上界、schema 清理与实际结果追加到本页。服务只绑定回环地址，临时数据库和浏览器证据位于仓库外 private-data。

失败按独立记录定位：配置/预算拒绝不得入网，未知模型结果不得重发，数据库错误先保留预占并拒绝新增知识；原本规则缺项先记录影响并继续有依据的部分。只有真实调用输出、持久状态和用户可查看的界面共同通过，才记该关键链路通过；商业整局和最终 MVP 仍需各自范围的证据。

## 实际验证（2026-09-06）

`backend/scripts/real_package_play_smoke.py` 默认预审零调用。固定全新虚构资料、真实来源定位和候选导入；发布权限是明确标识的 `SIMULATED_FIXTURE_ONLY`，不代表真实人审发布。执行必须同时提供 `--execute --confirm-paid ONE_SYNTHETIC_PACKAGE_PLAY_CALL`，并检查既有付费开关；每项最多一次模型请求、输出最多 256 token、预算上限不超过 ¥0.01。没有任意问题/素材/数据库参数，也不支持重发历史任务。

本轮预审目录 `private-data/import-jobs/package-play-smoke/20260905T203604Z-7jfemr3m` 位于仓库外。输入为 2040 UTF-8 字节，加 4096 token 协议预留后按 6136 输入、256 输出预占 ¥0.0054208。2026-09-06 经浏览器直接核对[火山方舟官方价格](https://docs.volcengine.com/docs/82379/1544106?lang=zh)，character 输入不超过 32k 时输入 ¥0.8/百万 token、缓存命中 ¥0.16、输出 ¥2，与冻结价格一致；仅保存对应价格行，未保存账号页面信息。

唯一真实执行目录为 `private-data/import-jobs/package-play-smoke/20260905T203637Z-kqeqvuya`。命令：

```sh
backend/.venv/bin/python backend/scripts/real_package_play_smoke.py --provider volcengine_ark --max-cost-cny 0.01 --execute --confirm-paid ONE_SYNTHETIC_PACKAGE_PLAY_CALL
```

退出码 0，`PASSED`。实际模型为 `doubao-seed-character-260628`，735 输入、14 输出、0 缓存/思考 token，按冻结价格估算 ¥0.000616（不是供应商账单），全烟测耗时 1676ms。输入仅含公开地点与乙可分享的封条证据；KEEP_PRIVATE、另一角色私本、后期材料、系统真相均未入模型。模型选对乙的证据，服务端正式分享并连续解锁 2 条公开证据与 1 条 CLAIM，再推进 2 个阶段，显式 SETTLE 仅揭晓指定真相。最终 1 次真实调用、5 条事件；新数据库连接重读与最终视图相同。此单例证明受限材料选择链路，不代表自然对白或商业整局质量。

`backend/scripts/test_package_play_postgres.py` 只接受精确本机测试库身份与显式确认值，拒绝 PG* 环境重定向、额外 URL 参数或任意 pytest 路径。仅在本进程新建的随机 schema 操作，清理时校验 schema 名称、OID 与所有者；不含 public 搜索路径、不读取 .env、不使用真实模型。

```sh
PACKAGE_PLAY_POSTGRES_TEST_CONFIRM=CREATE_PACKAGE_PLAY_EPHEMERAL_SCHEMA \
FUSION_POSTGRES_TEST_DATABASE_URL='postgresql+psycopg://fusion_it@127.0.0.1:55432/fusion_pg_it_sandbox' \
backend/.venv/bin/python backend/scripts/test_package_play_postgres.py
```

退出码 0，9 passed、13 条存量弃用警告，1.40 秒。实际应用 j0→o5 全部新增迁移并验证上下行与 ORM 一致；不同 PostgreSQL backend PID 验证同 revision/幂等请求不重复推进或预占、等待模型时不持两道行锁、阶段/发布状态变化后迟到答复仅记已知费用、来源门禁锁前变化复核、回滚重试和已提交预占的新连接恢复。异常中断用关闭会话后新建会话模拟，未杀进程；人审发布/来源文件是模拟 seam，未宣称完整真实发布链。每个临时 schema 已由入口核验清理，未迁移主 schema。

新增烟测离线 23 项与 PostgreSQL 入口守卫 42 项已计入完整后端离线检查：1443 passed、363 条存量弃用警告，12.41 秒。开发预检退出码 0，自测 16 项通过，diff 空白检查通过。

真实烟测产物经实际 `PackagePlayService.get`、SQLite `mode=ro`/`query_only` 与明确模拟认证/发布完成浏览器回看。1440px/390px 回答、CLAIM 分类、指定唯一真相、749 token 与估算费用 ¥0.000616 显示正确；无横向溢出，结尾后没有新操作控件，按钮刷新/重载/越权拒绝后恢复一致。实际 GET 与 `settled-view.json` 相同；9 项身份/写方法检查通过，13 份原始文件哈希未变，仍 1 局/5 事件，新增 SDK 构造/模型调用/出网均为 0。私有证据为同目录 `browser-verification.json` 与 3 张截图。

浏览器空间 61 已独立确认 `done:true` 关闭；临时 API 18018/前端 13018 均退出 0 并实查端口关闭。此阶段未修改前端源码，沿用此前 4B 已验生产构建，未重复构建或部署；随后已进入[4D 共享行动点与调查](PHASE_4_BUDGETED_INVESTIGATION.md)。

## 原本规则核查

采用 juben-design 的来源审查部分，报告保存在仓库外 `private-data/import-jobs/rule-review/20260905T203543Z-7bc13e6d`。记录 28 条机制、9 项缺口，读取的 22 个源文件未改动；13 个选中源与冻结包相符，14 段 OCR 引用的图片哈希吻合。本轮为文本与来源一致性核查，没有声称全套原图逐字校对或真人试玩。

原始材料支持两轮 22+9 共享行动预算、调查/钥匙前置、分阶段私读、他人发言触发私人回忆以及封卷后按角色目标计分。修订 v1.0 另外补充信任票上限、主案归票与多数阈值、结局优先级和分项评分口径；相关文件自标 `editor_added_not_original`，不能当成已获用户最终批准的原作规则。原 49 卡仍缺失，49-R 为已有整理授权的明确补充候选。

四份混合原文与编辑补充的关键聚合文档还缺少细粒度原件关系，旧 483 文件快照保持不变。先制作独立来源 overlay 和可审核版本建议；后续一次确认具体修订规则与补充范围。原作的“回忆原卡不可展示、允许转述”不能用 4B 原文公开替代，答卷的语义正误也不能让模型自行填正确标记直接计分。自然对白、私密回忆、扣时调查、私密答卷/信任票及分支结局仍是后续实现，以上核查没有批准商业发布。
